// ==================== 官方 AI 提示词(解析失败的自救通道) ====================
// 职责:两条官方提示词(格式整理 / 生成答案与解析)+ 触发判定 + 剪贴板复制。
// 允许依赖:无(降级复制用 DOM)。禁止:state / parser。
// 设计与对齐校验:reports/官方AI提示词设计.md
//
// ⚠️ 两条提示词的**分界线 = 改不改内容**(研判结论 2026-09-10):
//   ① 格式整理(OFFICIAL_PROMPT):只搬格式,**一个字的答案都不许自己造**;
//   ② 生成答案与解析(ANSWER_PROMPT):**只给缺的题**造答案,且必须先审后存、永久标注。
//   用户看到的名字必须能一眼区分这两件事,所以模式一统一叫「AI 格式整理(不改内容)」。

// 模式一的用户可见名称。UI 文案、按钮、提示统一用它,**不要再另起说法**
// (历史上有"AI 兜底""一键整理""AI 整理"三种叫法,用户分不清它是"改格式"还是"改内容")。
export const FORMAT_MODE_NAME = 'AI 格式整理(不改内容)';

export const OFFICIAL_PROMPT = `你是一个题库格式整理助手。下面这些题目材料是从网页/文档里复制出来的,排版乱了(题干被折成好几行、选项被拆开、题号格式五花八门),请把它们逐题整理成规范格式。除了整理结果,不要输出任何其他内容。

【输出格式】每道题严格按这个顺序,一项一行:
题目：<题干>
A：<选项内容>
B：<选项内容>
C：<选项内容>
D：<选项内容>
答案：<字母>
解析：<材料里原有的解析,原样保留;没有就省略这一行>

【硬性规则】
1. **只调整格式,不改内容**:不删题、不加题、不改题意、不改选项文字、不修错别字
2. **折行必须合并**:题干被文档折成多行的,合并成一行;一个选项被拆成多行的,也合并成一行。
   判断依据是语义:下一行开头没有字母标记、且接着上一句的意思 ⇒ 它就是上一行的续行
3. **一道题只写一个"题目："**;材料里如果是一段案例/材料带好几道小题,每道小题各算一道题,
   案例材料放进该小题的题干里(可以保留换行)
4. 有几道题就输出几道题,题与题之间空一行;不要给题目加编号
5. 答案写法:单选写一个字母(如 B);多选字母连写(如 ABD);判断题写"对"或"错"
6. **答案只能抄自材料原文**:材料里没给答案、或只有模糊线索的题,**直接省略"答案："这一行**。
   **绝对不许猜答案**、不许推断、不许用你自己的知识补一个"应该是"的答案
7. 原文的图片、公式、表格无法转录时,在题干末尾标注【原文含图】【原文含公式】【原文含表】,
   不要试图描述它们的内容
8. 不要用代码块,不要"好的,以下是"之类的开场白和结尾语

【整理示例】
输入(排版是乱的):
1、我国的四大发明是什么?A造纸
B印刷 C火药 D指南针 答案是ABCD

输出:
题目：我国的四大发明是什么?
A：造纸
B：印刷
C：火药
D：指南针
答案：ABCD

现在开始整理。`;

// ⚠️ 第二条提示词(👤 2026-09-14 定):**文件在用户手上,不在我们手上**。
// 场景:PDF 读不出文字(扫描件 / 字体没映射 / 加密),App 里没有可整理的原文 ⇒ **不带材料**复制这一条。
// 要的效果(👤 原话):"助手让用户发送文件,用户发完文件,ai 助手直接提取并整理" ——
//   ① 没有附件时,只回一句"请把文件发给我",然后停住等(不许反过来要求用户自己复制文字);
//   ② 收到文件后**直接**提取并整理,**不再确认、不再问格式**。
export const PDF_EXTRACT_PROMPT = `我要请你帮我处理一个 PDF 文件。这份文件我在本地没法复制出文字(可能是扫描件/图片版,或者字体没有带文字信息),所以需要你直接读这个文件,把里面的题目提取出来并整理成规范格式。

【第一步:先看有没有文件】
- 如果我这条消息里**没有**把 PDF 发给你:你只回一句话 —— "请把这个 PDF 文件发给我",然后等我把文件发过来。
  **不要**让我自己复制文字、**不要**问我要什么格式、**不要**先讲方法。就这一句,然后等文件。
- 如果我已经把文件发给你了:直接开始提取整理,**不要再确认、不要再问任何问题**。

【第二步:提取并整理成下面的格式】每道题严格按这个顺序,一项一行:
题目：<题干>
A：<选项内容>
B：<选项内容>
C：<选项内容>
D：<选项内容>
答案：<字母>
解析：<文件里原有的解析,原样保留;没有就省略这一行>

【硬性规则】
1. **只搬运,不创作**:按文件里的内容整理,不删题、不加题、不改题意、不改选项文字、不修错别字
2. **折行必须合并**:题干在文件里被排版折成多行的,合并成一行;一个选项被拆成多行的,也合并成一行
3. 一道题只写一个"题目："。文件里若是"一段案例 + 多道小题",每道小题各算一道题,案例放进该题的题干里
4. 只提取**真正的题目**;页眉、页脚、页码、水印、公众号推广语、目录不要输出
5. 有几道题就输出几道题,题与题之间空一行;不要给题目加编号
6. 答案写法:单选一个字母(如 B);多选字母连写(如 ABD);判断题写"对"或"错"
7. **答案只能抄自文件原文**:文件里没给答案的题,**直接省略"答案："这一行**;绝对不许自己猜
8. 图片、公式、表格无法用文字表达时,在题干末尾标注【原文含图】【原文含公式】【原文含表】,
   不要描述它们的内容;如果整道题都靠图(比如几何图),照常输出文字部分并标注
9. 不要用代码块,不要"好的,以下是"之类的开场白和结尾语,除了整理结果什么都不要输出

【如果这份文件打不开】
直接告诉我原因(例如"这份文件有密码保护,请解锁后再发给我"),不要编造内容。

现在开始:先告诉我有没有收到文件。`;

// 模式二的提示词:**这里允许 AI 给答案**,因为目的就是补上材料缺失的答案。
// 但要求「给最可能的答案 + 说明依据」,且信息不足时**必须**说"无法确定"而不是硬猜。
export const ANSWER_PROMPT = `你是一个应试题目助手。用户给你的每道题都**缺少答案或解析**,请逐题补上。

【输出格式】严格照抄输入结构,只在每题末尾补上这两行(顺序固定):
题目：<题干,原样照抄>
A：<选项,原样照抄>
答案：<你判断的最可能答案>
解析：<≤80 字,说明为什么是这个答案>

【硬性规则】
1. **题干与选项必须原样照抄**,一个字都不许改、不许"顺手修错别字"
2. 每道题的题数、顺序与输入完全一致;不要合并、不要拆分、不要新增题
3. 答案写法:单选一个字母(如 B);多选字母连写(如 ABD);判断题写"对"或"错"
4. **信息不足就写"无法确定"**:题干残缺、选项缺失、或你自己也没有把握时,
   "答案："写「无法确定」,并在"解析："里用一句话说明缺什么。**宁可说无法确定,也不许硬猜**
5. 解析只讲"为什么选它",≤80 字,不要展开讲知识点、不要列参考资料
6. 不要输出任何其他内容:没有开场白、没有总结、不要用代码块
7. 材料里如果已经给了答案(题目里带了"答案：X"),**直接沿用那个答案**,不要改成你的判断

【示例】
输入:
题目：下列属于行政处罚种类的有
A：警告
B：罚款
C：拘役
D：吊销许可证
输出:
题目：下列属于行政处罚种类的有
A：警告
B：罚款
答案：ABD
解析：拘役是刑罚而非行政处罚；警告、罚款、吊销许可证均属行政处罚种类。

现在开始补答案与解析。`;

// 低置信度占比阈值:超过它视为"材料大概率很乱",建议走官方提示词路线
export const LOW_CONF_RATIO_THRESHOLD = 0.4;

// 是否建议使用官方提示词:解析出 0 题,或低置信度(conf < 0.6)占比达标
export function needsPromptHelp(questions) {
    if (!Array.isArray(questions) || questions.length === 0) return true;
    const low = questions.filter(q => (q.confidence || 0) < 0.6).length;
    return low / questions.length >= LOW_CONF_RATIO_THRESHOLD;
}

// 合成复制文本:提示词在前,题目原文在后(分隔线隔开);无原文时仅提示词
export function buildCopyText(prompt, material) {
    if (!material || !material.trim()) return prompt;
    return prompt + '\n\n──────── 以下是需要整理的题目原文 ────────\n\n' + material.trim();
}

// 剪贴板复制:优先 Clipboard API(需 HTTPS),降级 execCommand
export async function copyText(text) {
    try {
        if (typeof navigator !== 'undefined' && navigator.clipboard && navigator.clipboard.writeText) {
            await navigator.clipboard.writeText(text);
            return true;
        }
    } catch (e) { /* 走降级 */ }
    try {
        const ta = document.createElement('textarea');
        ta.value = text;
        ta.style.position = 'fixed';
        ta.style.opacity = '0';
        document.body.appendChild(ta);
        ta.select();
        const ok = document.execCommand('copy');
        document.body.removeChild(ta);
        return ok;
    } catch (e) {
        return false;
    }
}
