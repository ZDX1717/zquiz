// ==================== 应用版本号 ====================
// 职责:一个常量 —— 界面上要显示的版本号就取这里,**唯一来源**。
// 允许依赖:无(纯常量)。禁止:任何 import。
//
// ⚠️ 它必须与 `package.json` 的 `version` **完全一致**(守卫在 `tests/dom-structure.test.js`:
//    两个数字不一致就直接报红)。规矩见 `继续开发Zquiz.md`:版本号永远是"已发布的事实" ——
//    没发版的批次不要动这里,发布时才跟着 package.json 一起改。
export const APP_VERSION = '0.15.0';

// 首页信息模块里那句「本次更新」(👤 2026-09-14:模块改成展示**当前版本的更新内容**)。
// ⚠️ 三条规矩:
//   ① 与 APP_VERSION **同源、同改** —— 发布时两行一起动,别只改版本号忘了这句;
//   ② **短**:上限 32 字(守卫会拦)。实测手机上 **≈24 字**还能一行放下(360px 屏也一行),
//      再长就折成两行 —— 虽然放得下,但信息模块会从"两行"变"三行",手机一屏的余量也跟着少一档;
//   ③ 只写**这一版真正上线的东西**,用 ` · ` 分隔,不写括号补充(站内文案规矩)。
export const RELEASE_NOTE = 'PDF 直接导入 · 首页改版 · 图标修复';
