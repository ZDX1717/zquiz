// ==================== 应用版本号 ====================
// 职责:一个常量 —— 界面上要显示的版本号就取这里,**唯一来源**。
// 允许依赖:无(纯常量)。禁止:任何 import。
//
// ⚠️ 它必须与 `package.json` 的 `version` **完全一致**(守卫在 `tests/dom-structure.test.js`:
//    两个数字不一致就直接报红)。规矩见 `继续开发Zquiz.md`:版本号永远是"已发布的事实" ——
//    没发版的批次不要动这里,发布时才跟着 package.json 一起改。
export const APP_VERSION = '0.15.0';
