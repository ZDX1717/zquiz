# DESIGN.md — Zquiz 设计规范(v1)

> 本文件是 Zquiz 所有 UI 改动的**唯一视觉宪法**。AI 会话与人工改样式前必读;
> 与本文件冲突的样式一律以本文件为准。令牌定义于 `styles.css` 顶部的 `:root`。
> 修订规则:改规范 = 改本文件 + 同步 `:root`,一个 commit 完成,不允许只改 CSS 不改文档。

## 0. 修订记录

- **v30(2026-09-12)** 新增 §3.10:PWA manifest + 图标(圆角方底 + 白色 Z、maskable 安全区、touch icon 不透明、
  `start_url/scope` 必须相对路径、`theme_color` 取页面底色);明确本批**不含** service worker。

- **v29(2026-09-12)** 修:配色卡片的脚部键(错题/收藏)展开后"白字压白底"看不见 —— 半透明白底的覆盖漏了`:not(.open)`。见 §3.4。

- **v28(2026-09-12)** 「导出题库」逐库写分节标题、**导入时自动认出多题库**(默认分开导入);回收站点外面收起。新增 §3.9。

> **只留最近几条**(每条一两行);完整过程记录(含更早版本)在 `archive/DESIGN-history.md`。
> 本文件只保留**现在仍然有效的规则**,规则写在正文各章,这里只说"这轮改了什么"。

- **v27(2026-09-12)** 列表交互定稿:**单击高亮 / 双击进编辑卡片**;去掉列表行与编辑卡片的左侧色条(编辑卡片原名 `.question-card` 撞上错题卡的红条类名,已改名 `.edit-card`)。⚠️ 单击**只换高亮不重画列表**,否则 `dblclick` 永远不触发。**设计规范自身也做了一次瘦身**:修订记录归档 + §3.5/刷题状态栏两章重写为当前口径。
- **v26(2026-09-12)** 单击高亮/双击选中/复选框圈选喂「只看勾选」;批量栏去掉「编辑」键;工具行 sticky 悬浮;筛选面板移出可横向滚动的工具行(否则被裁)。
- **v25(2026-09-12)** 控制条按钮矮一档(工具键 24/34、chip 22/30);筛选面板改悬浮;手机列表取消内滚;桌面删掉 220px 两栏残留;批量栏并入工具行;删掉"共 N 题"。
- **v24(2026-09-12)** 题目列表重构(复选框 + 徽章 + 筛选 + 批量栏),六个字段搬进编辑卡片(二级弹窗 1100)。
- **v23(2026-09-12)** 题库页头部一行三键(标题 · 导出题库 · ＋ 创建题库 · 🗑 回收站),三键同款胶囊、只有创建键实心。
- **v22(2026-09-12)** 「导出全部」→「导出题库」,搬到题库页头部(创建键左边)。

<details><summary>更早的版本(一行式索引;完整记录见 archive/DESIGN-history.md)</summary>

- v21(2026-09-12) 题库设置重新排版:按作用对象分块 + 删除题库独占危险区
- v20(2026-09-12) 删掉「当前题库:xxx」;修"改名后编辑器不刷新"
- v19(2026-09-12) 「保存」跟着选中的那一道走(行内 ✕/保存)
- v18(2026-09-12) 修:二级弹窗压在一级之上;删库后回收站当场刷新
- v17(2026-09-12) 回收站改成「我的题库」右侧的悬浮菜单
- v16(2026-09-11) 版本记录:补删除单条、修正存版时机、迁入题库设置
- v15(2026-09-11) 表单文案在输入框左边;判断题 A=正确/B=错误
- v14(2026-09-11) 编辑器再收敛:取消底栏/上一题下一题/全部折叠
- v13(2026-09-11) 编辑器两标签页 + 头部收矮 44px
- v12(2026-09-11) 题库编辑器重构(手机优先)
- v11(2026-09-11) 题库卡改成"配色底 + 框"(去掉左侧 4px 主色条)
- v10(2026-09-11) 错题卡与收藏卡合并为 .question-card
- v9(2026-09-11) 题干上方标签行统一高度(--tag-row-h)
- v8(2026-09-11) 修:手机上题干被压成窄柱(auto 边距吃空间)
- v7(2026-09-11) 修:状态栏按钮文案变宽把整条挤高
- v6(2026-09-11) 修:套题模式交卷前答题卡泄露对错
- v5(2026-09-11) 刷题状态栏定稿(收藏移到标签行)
- v4(2026-09-11) 答题卡抽屉规范
- v3(2026-09-10) 色彩令牌全量重校准
- v2(2026-09-10) 采纳 slate/emerald/rose/amber 色阶,卡片 12px + 细边框 + 微阴影
- v1(2026-09-10) 首版

</details>

## 1. 设计立场

- **工具,不是展品**:刷题效率优先,装饰归零。密度可以高,但不能挤。
- **规则,不是品味**:所有数值来自下面的刻度表,禁止随手写 px。
- **功能性用色合法**:🤖 紫 / ⏳⚠ 橙 / 🕘 灰 / 状态红绿是**语义标记**,不受"单强调色"约束;但**装饰性**用色仍然禁止——页面上同一时刻只允许蓝色一个"行动色"。

## 2. 色彩令牌(唯一色彩来源 = `:root` 变量;v2 起中性色 = slate 阶,反馈色 = emerald/rose/amber)

> ✅ **本表已于 v3 按实测值全量对齐 `styles.css`**,可直接照抄。若与本表不符,以 `:root` 为准,并**立即回来修本表**(见第 0 节 v3 事故)。

| 令牌 | 亮色(`:root`) | 暗色(`html[data-theme="dark"]`) | 用途 |
|---|---|---|---|
| `--c-bg` | `#f5f7fa` | `#121417` | 页面底 |
| `--c-surface` | `#fff` | `#1c1f24` | 卡片/表面 |
| `--c-surface-alt` | `#f8f9fa` | `#22262c` | 次级表面/嵌入区 |
| `--c-text` | `#343a40` | `#e9ecef` | 主文字 |
| `--c-text-secondary` | `#495057` | `#ced4da` | 次级文字 |
| `--c-text-muted` | `#6c757d` | `#adb5bd` | 弱文字(**对比度下限,禁再浅**) |
| `--c-text-faint` | `#adb5bd` | `#868e96` | 装饰性弱文字(时间戳等,不承载必读信息) |
| `--c-border` | `#dee2e6` | `#3f444b` | 边框 |
| `--c-border-light` | `#e9ecef` | `#2c3138` | 浅边框/分隔线 |
| `--c-primary` | `#2563eb` | `#3b82f6` | **唯一行动色**:主按钮/链接/选中 |
| `--c-primary-deep` | `#1d4ed8` | `#60a5fa` | 主色深阶(hover/按压) |
| `--c-success` / `-deep` | `#28a745` / `#2e9e5b` | `#3fbf6a` / `#4cd07d` | 正确/成功 |
| `--c-success-bg` / `-text` | `#d4edda` / `#155724` | `#1d3a26` / `#8fe3a8` | 成功底/文字 |
| `--c-danger` / `-deep` | `#dc3545` / `#d9534f` | `#e05260` / `#e8737f` | 错误/危险/删除 |
| `--c-danger-bg` / `-text` | `#f8d7da` / `#721c24` | `#43202a` / `#ff9a9a` | 错误底/文字 |
| `--c-warn` / `-text` | `#ffc107` / `#7a5c00` | `#ffd666` / `#ffd666` | 警告 |
| `--c-warn-bg` / `-accent` | `#fff3cd` / `#b8860b` | `#423a15` / `#e0b34c` | 警告底/强调 |
| `--c-accent-purple` / `-bg` / `-text` | `#8e7cc3` / `#efeaff` / `#6a4fb6` | `#a08fd6` / `#2a2440` / `#c3b3ff` | 🤖 AI 动过(未核验) |
| `--c-info-bg` / `-border` | `#eef3fb` / `#b9c6dd` | `#1e2632` / `#3a4a63` | 信息位/提示块 |
| `--c-panel-bg` | `#f1f3f5` | `#262a30` | 面板底(🕘 历史 chip 等) |
| `--c-surface-warm` | `#fffdf5` | `#242320` | 暖底(救援区等) |
| `--c-warn-bg-soft` | `#fff8e6` | `#3a3320` | 柔和警告底 |
| `--c-success-soft` | `#ecfdf5` | `#0a2e1f` | 柔和成功底 |
| `--c-danger-soft` | `#fff1f2` | `#3f0d18` | 柔和危险底 |

**规则**:
- 新样式**禁止出现裸 hex**,必须引用令牌(一次性 tint 色需先入令牌表)。
- `--c-text-faint` 只允许用于非必读信息;必读文字最低 `--c-text-muted`(AA 4.5:1)。
- 反白按钮文字用 `#fff`,不令牌化(亮暗通用)。

### 2.1 用途专用令牌(收口裸 hex 的产物,v3 新增 14 个)

这些令牌**亮色值 = 收口前的硬编码原值**(逐条机械比对,零视觉变化),暗色值补齐后使这些用途首次可换肤。新代码遇到同类用途请复用下表,不要再引入新色。

| 令牌 | 亮色 | 暗色 | 用途 |
|---|---|---|---|
| `--c-primary-hover` | `#5a6268` | `#4a5058` | secondary 按钮 hover(中灰) |
| `--c-text-secondary-deep` | `#2c4a80` | `#a8c0e8` | info 提示文字(深蓝) |
| `--c-warn-text-deep` | `#7a5800` | `#ffd666` | 告警提示文字 |
| `--c-warn-border` | `#e0a800` | `#8a7333` | 告警提示边框 |
| `--c-badge-warn-text` / `-border` | `#856404` / `#ffeeba` | `#ffd666` / `#5c4f1f` | ⚠ 徽章 |
| `--c-warn-icon` | `#f0a020` | `#e0b34c` | ⏳ 待修左条(暖橙) |
| `--c-warn-text-strong` / `-alt` | `#7a5200` / `#b45309` | `#ffd666` / `#e0b34c` | 待修文字 / 双标说明 |
| `--c-star-border` / `-text` / `-bg` | `#fd7e14` / `#b4690e` / `#fff8e1` | `#f0a500` / `#ffd666` / `#3a3320` | ☆→★ 收藏三态 |
| `--c-star-hover-bg` | `#fffaf0` | `#302a1c` | ☆ 收藏 hover 底 |
| `--c-option-exp-text` | `#fd7e14` | `#ffd666` | 选项解释文字(逐题模式) |
| `--c-danger-hover` | `#c82333` | `#ff8a94` | 删除按钮 hover |
| `--c-input-border` | `#ced4da` | `#3f444b` | 表单输入边框 |
| `--c-review-unanswered-border` | `#c3e6cb` | `#2f5c3d` | 回顾·未作答行边框 |
| `--c-selected-text` / `-strong` | `#2c5282` / `#1c3d7c` | `#b9ccec` | 选中行文字 / 待修+选中行文字 |
| `--c-selected-bg-strong` | `#e7f0ff` | `#26313f` | 待修+选中行底 |
| `--c-accent-purple-faint` / `-tint` / `-overlap` | `#fbfaff` / `#fbf7ff` / `#b08df0` | `#232030` / `#2a2440` / `#7d68b8` | 🤖 行底 / 紫+⏳ 行底 / 双标左条过渡 |
| `--c-review-wrong-bg` | `#fff5f5` | `#3f0d18` | 回顾·答错行底 |
| `--c-ai-connected-text` / `-bg` | `#1e7e46` / `#e9f7ef` | `#8fe3a8` / `#1d3a26` | AI 已连接徽章 |
| `--c-border-hairline` | `#eef0f3` | `#2c3138` | 极浅分隔边框(救援区) |

> ✅ **已决(2026-09-10,👤 拍板)**:`--c-primary-alt` / `--c-primary-alt-deep` 已**并入 `--c-primary` / `--c-primary-deep`**,14 处引用一并改写,定义行已删。二者取值原本亮暗两档就完全相同(别名是"紫→宝蓝"过渡期的遗留,无独立语义),故**本次合并零视觉变化**。同时删除零引用的 `--c-chip-bg`。令牌总数 66 → 63。
> **现状:全站已无未被引用的令牌**。新增令牌后请用这条自检:`for t in $(grep -o '^\s*--c-[a-z-]*' styles.css | tr -d ' ' | sort -u); do [ "$(grep -c "var($t)" styles.css)" = 0 ] && echo "未被引用: $t"; done`

## 3. 语义标记色(AI/待修/历史,永久标注体系)

| 语义 | 色 | 形态 |
|---|---|---|
| 🤖 AI 动过(未核验) | 紫 `--c-accent-purple-*` | 列表左条 3px + 徽章 |
| ⏳/⚠ 待修 | 橙 `--c-warn-icon` / `--c-warn-text-strong` / `--c-warn-bg-soft` | 列表左条 3px + 徽章 + 底色 |
| 🕘 历史(仅 × 删) | 中性灰 `--c-panel-bg` 系 | 小 chip,不做左条 |
| 对/成功 | `--c-success-*` | 反馈/状态条 |
| 错/危险 | `--c-danger-*` | 反馈/状态条/删除 |

左条优先级:待修(橙) > AI(紫) > 历史(灰),并存时用过渡色,禁止叠加三条。

## 3.1 信息架构:三页面职责边界(👤 2026-09-10 定调,改按钮位置前必读)

> **首页 = 导入;刷题 = 一切"做题";题库 = 一切"管理"**

| 页面 | 只放什么 | 明确不放 |
|---|---|---|
| **首页** | 导入卡(粘贴/选文件/解析预览)、AI 救援区 A·B、格式说明(折叠)、导入撤销 | **任何"开始做题"按钮**、功能介绍/宣传语、版本号、主题开关(已移入标题栏) |
| **刷题** | 题源(题库/错题本/收藏夹)、选择题库、题型、作答模式、随机、开始刷题 | 题库管理动作 |
| **题库** | **头部(一行):我的题库 · 导出题库 · ＋ 创建题库 · 🗑 回收站**;库卡(名字/徽章/错题/收藏/版本);编辑(重命名/导出本库/去重/删除题库/清空错题/移出规则) | **开始刷题**、复习入口 |

**判定规则**:一个按钮属于哪个页面,看它**产出什么**——
- 产出"一场作答" → 刷题页(哪怕题源是错题本/收藏夹)
- 产出"题库数据变更" → 题库页/编辑器
- 产出"题目入库" → 首页

**"复习"不是独立功能,而是题源的取值**:错题本/收藏夹与题库并列构成刷题的三种题源,
范围由「题源 + 选择题库 + 题型」共同表达。因此:
- **「选择题库」常驻,任何题源下都不隐藏**(👤 验收反馈:让它消失会造成布局跳动)。
  标签随题源改写:"选择题库:" / "错题所在题库:" / "收藏题所在题库:"
- 三种题源下「选择题库」都生效,但**实现方式不同**:
  - 题库题源:由下拉框 change 直接收窄 `state.questionBank`,**不得再按 `bankName` 过滤**
  - 错题/收藏题源:这两类入库时写入了 `bankName`,按它收窄
- ⚠️ **关键数据事实**:导入的题**不带 `bankName` 字段**(归属靠"在哪个库数组里"表达),
  所以对题库题源做 `bankName` 过滤会把整库题滤光。改这段前务必看 `startQuiz` 的注释。
- 复习范围弹窗已退役(曾因藏在编辑器弹窗与隐藏 section 里导致"点了没反应")

**首页不放版本号的理由**:版本属于发布信息,放首页会随每次发版变成噪声;README 与
`目标.md` 才是版本的事实来源。

## 3.2 刷题两屏:设置页与完成页(👤 2026-09-10 定调:简约易读、压行数)

### 刷题设置(`.quiz-settings`)

**顺序 = 决策顺序,全部裸露**:

```
[ 题库 | 错题本 | 收藏夹 ]   ① 题源(每次都要选)
题库  [全部题库        ▾]    ② 范围(标签随题源改写)
[ 逐题 | 套题 ]              ③ 模式
[ 全部 | 单选 | 多选 | 判断 ] ④ 题型
☑ 随机出题顺序               ⑤ 顺序
[        开始刷题        ]
```

规则:
- **选项文案不得带解释性括注**。`逐题` 就是 `逐题`,不写"逐题模式(每题立即出答案,作答时显示解释)"
  —— 一句话能说清的事不该占两行;需要解释时用一次性引导,不在每次刷题时重复。
- 多选一控件用**分段控件** `.seg`(一行多选一,原生 radio 隐藏、选中态由整段高亮表达),
  不再用竖排 `<label>` 列表。`.seg` 必须保留 `:focus-visible` 描边。
- **禁止把刷题设置项收进 `details`**(👤 2026-09-10 反馈:折叠"碍事")。
  曾把题型与随机收进「更多设置」,被真机验收否决——刷题前调整是高频动作,
  多一次展开就多一次打断;**压行数只能靠控件形态(分段控件/单行文案),不能靠藏功能**。
- 一个 `.seg` 在源码里写成一行(每个 `<label>` 内含 input+span),避免每条选项占 4 行。
- 单开关(如随机顺序)用 `.setting-check`,须显式 `flex: none`(否则被 `.seg label` 的 `flex:1` 拉满)且 `min-height: 40px`。

### 刷题状态栏(`.quiz-controls`)

> 术语:刷题界面下方那条按钮栏 = **刷题状态栏**(文档/提交信息/对话统一用这个名字)。

```
题干上方标签行(.question-tags,一行三件、从左到右、禁止折行):
[ 3/45 ▾ ]  [ 单选题 ]  [ ☆ 收藏 ]
题干文本…
选项…

刷题状态栏:
[ 智能切题 ]                    [ 上一题 | 下一题 | 交卷 ]
```

**布局铁律**
- `#next-question-btn` 必须 `min-width:76px`(装得下「确认答案」)+ 文字居中 —— 它有两种文案,
  不锁宽就会"文案变长 → 按钮变宽 → 左区折行 → 整条变高"。**任何会随内容变宽的按钮都不得进左区。**
- 左区 `.quiz-meta > .quiz-status > .auto-next` 只剩「智能切题」一个定宽按钮(`flex:1 1 auto` 占位、
  按钮 `flex:0 0 auto`);套题模式下它隐藏,左区为空。手机档**不要**给左区设 `nowrap`/`overflow:hidden`
  (那只会挤掉文字,防折行靠"左区没有可变宽内容")。
- 标签行 `.question-tags`:`nowrap` + 每个子项 `flex:0 0 auto`。🚨 **这排的宽窄变化不得影响题干宽度**
  (题干是块级 `<h3>`,宽度取父容器 100%,与这行放了什么无关)。
- 三件**等高**:高度只从令牌 `--tag-row-h` 取(28px,手机 26px),字号一律 12px;用 `inline-flex` +
  `align-items:center` 让 `min-height` 生效。⚠️ 答题卡入口**不得 `font:inherit`**(会继承 16px 而后不齐),
  写死 `font-size`。有守卫:写死高度 / 三件不一致即失败。
- 「收藏」在标签行第三位,⚠️ 它自带的 `margin-left:auto` **必须清掉**(会把整行撑开)。
- 「智能切题」= 带框胶囊,开关态用主色描边 + `--c-info-bg` + `aria-pressed`;默认关闭,存本机
  (`autoNextSetting`,脏值回退关闭)。
- 答题卡入口 `.answer-card-open` = 标签行最左「1/45 ▾」+ `aria-haspopup="dialog"` —— 它既是**唯一的进度显示位**
  (`#question-number` 在它内部),又是抽屉开关。

**行为**
- 套题模式:`上一题 → 下一题 → 交卷` 从左到右常驻,首/末题**只禁用不隐藏**(隐藏会像"两个键在循环")。
- 逐题模式:`上一题 | 下一题 | 结束刷题`,首题「上一题」禁用;**回看已作答的题不改分**。
- **未点「确认答案」就切题不得判分**:`prevQuestion` 只在本题确已判分(`state.isAnswered`)时才写入
  `userAnswers`;改用明确标记 `q_graded[index]` 判定"真的判过分"(曾用"记录是否为空"判断 → 没确认就判了分)。
- **回看要完整复现当时结果**:选项着色(绿/红 + 锁定)、反馈文案、解析一并恢复。反馈文案走**留档**
  `q_feedback[index]`(作答时存下文案与样式类),回看原样照搬 —— 不重推,因为当次可能带
  "已连对 N 次,移出错题本"这类上下文。开新一局清空留档。
- 末题「结束刷题 / 交卷」变色(`.ready`):

  | 模式 | 变色时机 |
  |---|---|
  | 逐题 | 末题**判完分**才变色 |
  | 套题 | 末题**选完**即变色(取消选择要恢复)——套题没有"判完分"这个时刻 |

  故**每次选项变动都要重算** `markEndButtonReady()`,且它读 `state.isAnswered`,**必须在判分之后调用**
  (反了会静默不变色)。
- **空作答不跳过**:点「下一题」/左滑时若一个选项都没选 → 行内提示并**留在本题**。
- 动效尊重 `prefers-reduced-motion: reduce`。

### 手势(仅手机触屏)

| 手势 | 行为 |
|---|---|
| **左滑** | 下一题(未作答的题按"确认答案"处理,即先判分再前进) |
| **左滑 · 最后一题** | **禁止** —— 收尾必须走「结束刷题」/「交卷」,避免误滑结束整局 |
| **右滑** | 上一题(两种模式都可回看;已作答的题已锁定,回看不改分) |
| **长按** | 收藏本题(手机) |

⚠️ **回看必须恢复"看得见的"选中态**:原生 radio/checkbox 是**视觉隐藏**的(见 P0-1.7),
选中态由 `.option-item.selected` 的卡片高亮表达。恢复作答时**只设 `inp.checked` 是不够的** ——
界面会"看不到刚才选了啥"(👤 反馈的 bug)。两处都要设。
左右滑动阈值:`|dx| ≥ 60` 且 `|dx| > |dy| × 1.5`(避免与竖向滚动冲突)。

#### 题型与答案一致性(数据规范)

> **答案是多字母 ⇒ 题型必须为多选。** 否则刷题按单选渲染(单选控件只能选一个字母),
> 该题**永远判不对** —— 这是"手动把答案从 A 改成 AC 却没改题型"造成的死题。

- `finalizeQuestion` 里按答案长度推导题型,**两种情况要区分**:
  - **导入路径**:材料写的「类型:多选」只是提示,与答案矛盾时**以答案为准**(判分自洽)。
  - **编辑器保存**:用户在下拉框里亲手选的类型是明确意图,**保留**(多选题只有一个正确项是合法形态)。
    由 `editorSaveCurrent` 打 `_typeExplicit` 标记区分,该标记**不落库**。
- `editorSaveCurrent` 的赋值顺序**不能反**:必须先把采集到的 `options/answer` 落到 `q` 上,
  再调 `finalizeQuestion(q)` —— 否则 finalize 读到的是旧值,纠正失效(踩过)。
- 保存后下拉框回显**真正落库的类型**;若被自动纠正,`alert` 明确告知(如「已按答案自动改为「多选」并保存」)。

#### 自动下一题(行为规范)

**仅逐题模式有此功能**(👤 决定):套题模式整组作答、交卷后才统一判分,作答时并不知道对错,
"答对才翻"无从谈起;一律自动翻又会让用户失去检查/回改的机会 —— **故套题模式下按钮直接不出现**。

| 场景 | 行为 |
|---|---|
| 开关关闭(默认) | **一律不自动翻页** |
| 逐题模式 · 答对 | 延迟 900ms 自动翻到下一题 |
| 逐题模式 · 答错 | **停留**在当前题(看解析、消化错因) |
| 套题模式 | **功能不适用**:按钮隐藏,判定恒为不翻 |
| 任意模式 · 最后一题 | **不自动翻页**(留给用户点「结束刷题」) |
| 手动翻页 / 结束 / 返回设置 / 开始新一局 | **取消**待执行的自动翻页(手动操作优先) |

- 延迟 **450ms**:切题要跟手(👤 反馈「快点」);450ms 足够看清判定色而不拖沓。
- 判定逻辑抽成**纯函数** `shouldAutoNext(isCorrect, {autoNext, quizMode, index, total})`,
  便于单测 —— vm 沙箱的 `setTimeout` 是空实现,定时器本身在测试里不会触发,只能测判定。

### 答题卡抽屉(`.answer-card-drawer`,P0-6.1 👤 定调)

**用途**:刷题中一眼看清"哪些做了、哪些错了、哪些还没做",点号直达任意一题。

- **入口 = 题干上方标签行最左的进度按钮**(`#answer-card-open`,内容为「3/10 ▾」)。
  理由(👤 决定):要跳题时视线本来就在这个数字上,在它身上开口 = 不必新增常驻元素。
  ⚠️ 但它**必须做成看得出是按钮的中号胶囊**(有框,26~28px)——迷你版"文字点亮"形态
  实测导致入口找不到,等于功能没做(👤 反馈)。
  右邻依次是「题型徽章」「☆ 收藏」;它**不住在状态栏里**,故不受「下一题/确认答案」宽度变化影响。
- **底部弹出**,`max-height: 62vh`,遮罩 `z-index: 1200`、抽屉 `1201`(必须高于弹窗的 1000 ——
  否则在弹窗里打开的答题卡会被盖住);点遮罩、点 ✕、按 Esc 都收起。
- **一格一题**:6 列网格(`aspect-ratio: 1`),每格只放**题号 + 一个题型角标**。
- **题型角标** = 单/多/判(左上角 `CARD_TYPE_SHORT`,9px);多选/判断一眼可辨,不必点进去看。

三态(颜色 + 记号**双通道**,不只靠颜色区分):

| 态 | class | 外观 | 出现时机 |
|---|---|---|---|
| 答对 | `correct` | 绿底绿框 + 深绿字 | **已判分**且答对 |
| 答错 | `wrong` | 红底红框 + 深红字 | **已判分**且答错 |
| 已选 | `picked` | 中性灰底 + 实线 + 左上角三角 | **套题交卷前**专用:选过了,但还没判 |
| 未答 | `blank` | **虚线框** + 灰字 | 没选过 |
| 当前题 | `current` | 额外 `outline: 2px solid var(--c-primary)` | 永远是当前题 |

- 🚨 **套题模式交卷前不得出现 `correct`/`wrong`**(👤 报的 bug)。
  套题是"整卷一次判分",交卷前用户仍可回改 —— 此时把对错标在卡上 = **直接在卡上泄题**,
  比看题干更糟(卡上直接告诉你哪题错了,等于给了答案范围)。故:
  - 判分事实由 `q_examGraded` 决定,它**只在 `finishExam()` 里置真**、在 `startQuiz()` 里清零;
  - 交卷前所有格子只能是 `picked` / `blank`;
  - 概览行同理:**交卷前只报"已选 N / 未答 M",不报对错数**。
  - `tests/answer-card.test.js` 有守卫:逐格断言交卷前既无 `correct` 也无 `wrong`
    (只断言"应为 picked"是不够的 —— 那样漏掉对错标记也能通过)。
- ⚠️ **"未答"的判据是"判过分没有",不是"userAnswers 有没有值"**。逐题模式下
  "多选勾了但没点确认"的题在卡上**不能冒充已答**(`q_graded` 才是判分事实)。
  踩坑记录:上一批刚修过"未确认的勾选被当成已判分"的 bug,答题卡若按 `userAnswers` 取态,
  等于把同一个 bug 换个地方复活。
- ⚠️ **抽屉里不得出现题干与答案**。跳题前看到答案 = 提前泄题,这条是硬约束,
  `tests/answer-card.test.js` 有守卫(断言格子数据里不含 `content`/`answer` 键)。
- **跳题是导航动作,不替用户交卷**:跳转不触发判分,只改 `currentQuestionIndex` 并收起抽屉;
  越界索引直接忽略。跳题后**状态栏进度必须同步刷新**(与翻页按钮共用同一套渲染路径)。

### 刷题完成(`.quiz-result`)

**数字即主张,标签退让**:

```
63.6%  正确率 · 已答 11 题      ← 24px 粗体,唯一视觉焦点
✓ 7 正确  ✗ 4 错误  ○ 0 未答  共 11 题    ← 12px chips,次要信息
```

规则:
- **只允许一个视觉焦点**:正确率 24px/700;其余统计一律 12px chip。
- 禁止用"每个统计一行"的 `<p>标签:值</p>` 排布(旧版 5 行,扫读无落点)。
- 正确率**按高低着色**(≥60% 绿 / <60% 红),给"好不好"一个即时判断;
  但**着色只用在正确率这一个元素**,其余 chip 用语义色(正确绿/错误红/未答灰)。
- 「已答 N 题」只在**分母 ≠ 总题数**时出现(逐题模式提前结束),套题模式不重复总题数。
- 「总题数」并入 chips(共 N 题),不与「已答」重复表达。
- **作答中与结果页都必须隐藏「开始刷题」按钮与配置区**,且该规则**必须全屏宽生效**,
  不能只写在 `@media (max-width: 768px)` 里 —— 否则桌面上会出现"结果页上方还挂着一个开始刷题按钮"。
  选择器需同时覆盖 `#quiz-container:not(.hidden)`(作答中)与 `#quiz-result:not(.hidden)`(结果页)。
  `tests/dom-structure.test.js` 有守卫:剔除全部媒体查询后该规则仍须存在。
- 结果页唯一的返回动作文案为 **「返回刷题主页」**(原「返回设置」)。它回到**刷题页的设置态**,
  即重新选择题源/模式/题型的地方;文案要说清"回到哪",不要让用户猜「设置」指什么。


## 3.3 标题栏与错题条目(👤 2026-09-10 定的改动)

### 标题栏(`header`)

- **常驻应用栏 + 悬浮吸顶**,桌面为**三栏**:左品牌、中导航、右主题开关
  (`grid-template-columns: 1fr auto 1fr`;**nav 必须是 header 的子元素**,否则它会作为独立一条
  横在标题栏与内容之间 —— 实测那一条占 63px,正是"导入题库卡片上方一大块空白"的主因)。
  手机端 nav 脱离为底部固定 tab 栏(媒体查询里恢复 `position: fixed` 并**显式覆盖**桌面的
  透明背景/白色文字/gap,否则底栏会变透明)。
- ⚠️ **header 必须用 flex,不能用三栏 grid**。手机端 nav 是 `position: fixed` 会脱离文档流,
  而 grid 仍会给它保留中列 → 品牌被挤半行、主题开关卡在中间,**整条标题栏排版错乱**
  (👤 实测反馈)。flex 下缺席的子项自动退出排布,桌面三件同排、手机两件两端对齐,两种形态都成立。
- ⚠️ **按钮类必须显式声明 `border`**(`border: none` 或自定义边框)。缺声明会露出浏览器
  默认按钮边框,表现为"按钮周围有黑边"。踩坑:合并重复的 `.nav-btn` 规则时把 `border: none`
  一起删掉了。`tests/dom-structure.test.js` 有守卫(判据需排除 `border-radius` —— 它含
  `border` 子串,否则守卫形同虚设)。
- **品牌块** = `h1.brand`(保留"页面唯一一级标题"语义)内含 `small.brand-tagline`:
  logo `Zquiz` 16px/600,tagline「期末周刷题助手」**10px,紧随 logo 后、落在其右下角**。
  实现要点:`h1.brand` 保持**行内流**(不得用 `flex-direction: column`)—— 小字作为行内
  `small` 与 logo 同行按基线对齐,天然贴在右下且**不单独占一行**;`margin-left: 6px` 给间距;
  `min-width: 0` 保证窄屏能收缩,不把主题开关挤出屏幕。
  ⚠️ 不要为了布局把 h1 换成 span —— 页面会失去一级标题(无障碍与文档结构)
- **收窄**:`padding: 8px 12px`,站名 16px/600 —— 标题栏是"路标"不是"门面",不占内容高度
- **悬浮**:`position: sticky` + `z-index: 20` + 底部阴影(阴影用于与内容分层,否则同色蓝底会看起来粘在内容上)。
  吸顶偏移:桌面 `top: 24px`(对齐 body 的 24px padding,避免上方露背景带)、手机 `top: 0`
- ⚠️ **`position: sticky` 会被祖先的 `overflow: hidden/auto/scroll` 裁剪而静默失效** ——
  故 `.container` **不得**设 overflow(曾为圆角裁切而写过)。`tests/dom-structure.test.js` 有守卫,
  且该守卫已验证"破坏前提时会真的失败"。圆角改由子元素各自处理。
- **所有分区都显示**,不再随分区隐藏(旧规则:手机上非首页隐藏;已删除)
- 主题开关住在蓝底上,故用**半透明白**(`rgba(255,255,255,.45)` 描边 / `.75` 文字 / 选中白底蓝字),
  **不用令牌色** —— 令牌是为浅色表面设计的,直接套会在蓝底上糊掉

### 题目卡片(`.question-card`,错题与收藏**共用一套设计**)

> 👤 定调 2026-09-11:错题卡与收藏卡**统一**;题型 / 收藏 / 删除一律放在**题干上方一行**。

结构(两张卡完全同构,只差修饰类和状态徽章):

```
.card-tags   [ ［判断］ ] [ 📕 在错题本 ]        [ ☆ 收藏 ] [ 删除 ]   ← 一行,题干之上
h4           题干
p.error-options  选项裸露(A. 甲　B. 乙)
details.answer-reveal 「查看答案」→ 你的答案 / 正确答案 / 解析(收藏卡只有正确答案)
```

- **基础类唯一**:两张卡都是 `.question-card`(修饰类 `.error-card` = 左条红、`.fav-item` = 左条星橙)。
  ⚠️ **不要给两张卡各起一个基础类** —— 那样任何按类选卡的代码/样式都会漏掉其中一种
  (踩过:收藏卡曾叫别的基础类,查询 `.error-item` 的代码直接漏掉它)。
- **标签行 `.question-card-tags`**:`flex` + **`nowrap`** + 子项 `flex: 0 0 auto`,
  与刷题页 `.question-tags` 同一条规矩 —— 这行按钮的宽窄变化不得影响题干宽度。
  题型在左;**收藏与删除靠右**(`收藏` 用 `margin-left: auto` 推过去),动词归动词、标签归标签。
- **尺寸与刷题页标签行同规**:三件(题型/徽章/按钮)统一 `min-height: var(--tag-row-h)`(28 / 手机 26)、
  字号 12px。状态徽章 `.badge` 在这个行内也要拉齐高度,否则会出现 19 vs 26 的落差。
- **删除按钮用"描边红"而不是实心红**:它紧挨收藏按钮,实心红会把整行视觉重心吸走
  (收藏才是这里的常用动作)。`--c-danger-hover` 随之零引用,已删除该令牌。
- **折叠态绝不泄露答案**:正确答案/你的答案一律写在 `details` 内部,卡片外层不得出现。
  收藏卡也走同一套遮挡(统一设计的另一面)。
- 判断题展示「对/错」而非 A/B:`formatAnswerForDisplay()` 是唯一展示入口。
- ⚠️ **`errorbook.js` 与 `favorites.js` 里各有一段同名旧渲染,目前是死路径**
  (`#errors-list` / `#favorites-list` 已不在 `index.html`),且其卡片是"答案直接摊开"的旧设计。
  它们仍被测试覆盖(测试桩会自动建出那两个容器,所以测试看起来在跑),**复活前必须先按本节重写**。

规则:
- **折叠态绝不泄露用户错选**:题型行不得出现「你答:X」这类标注。原因有二 ——
  ① 用户已经知道自己答错了,再标注一遍没有信息量;② 它会把注意力引向错误项,而不是正确答案。
  正确/错误对比只在 `details` 展开后出现(主动回忆遮挡)。
- **判断题展示「对/错」而非 A/B**:数据层统一存 A/B(判分一致),只在**展示时**用
  `formatAnswerForDisplay()` 映射。该函数是**唯一**的答案展示入口,新增展示位必须走它。
  现有展示位:刷题反馈、结果页逐题回顾、库卡错题面板、收藏条目、旧错题面板。
- **收藏 / 删除按钮同处一行**(`.error-actions`,右对齐)。手机端**不得**改成竖排。

## 3.4 题库卡配色(👤 定稿 2026-09-11)

**题库页的库卡**不再是"白底 + 左侧 4px 主色装饰条",而是**低饱和彩色底 + 同色系边框**;
默认(不设色)与站内其它卡片完全一致:**白底 + `--c-border` 边框**。

| `data-color` | 底色令牌 | 边框令牌 | 亮色观感 |
|---|---|---|---|
| (缺省) / `grey` | `--c-surface` | `--c-border` | 白底灰框,与其它卡一致 |
| `blue` | `--c-bank-blue` | `--c-bank-blue-border` | 淡蓝 |
| `green` | `--c-bank-green` | `--c-bank-green-border` | 淡绿 |
| `red` | `--c-bank-red` | `--c-bank-red-border` | 淡红 |
| `amber` | `--c-bank-amber` | `--c-bank-amber-border` | 淡琥珀 |
| `teal` | `--c-bank-teal` | `--c-bank-teal-border` | 淡青 |

规则:
- **底色必须低饱和**:卡片里还挤着状态徽章(`错 1 / 藏 2`)与底部按钮,底色一浓就抢内容。
  饱和度按"能一眼区分、但不抢戏"调;不要把现有值往浓里改。
- **底色与边框成对出现**,只在 `:root`(亮)与 `html[data-theme="dark"]`(暗)定义;
  卡片本体只写 `[data-color="..."]` 映射。**禁止内联样式或硬编码颜色**。
- **暗色必须另配一套同色相深值**:浅底在暗色下会刺眼(实测把浅色直接套暗色,卡片像发光块)。
- **卡片内的按钮用半透明白底**(`rgba(255,255,255,.62)`,暗色 `.08`),让它跟着底色自动适配 ——
  给每个色配一套按钮色是维护地狱。
  ⚠️ **这条覆盖必须写成 `:not(.open)`**(👤 2026-09-12 报的 bug):脚部「错题 / 收藏」的**展开态**
  是"实心主色 + 白字",若被半透明白底一起盖住,就成了**白字压在白底上** —— 浅色卡片上完全看不见
  ("点开后字体与背景区分不开")。规则:配色卡片只接管**未展开**的外观;展开态永远走主色实底(实测对比度 5.17:1)。
- **「灰」既是默认也是色板里的一档**;存数据时灰 = 缺省,`bankColors` 里不留字段(避免冗余)。
- 配色**按库名存储**(`state.bankColors`),因此:重命名库要迁移、删库要清除
  (否则重建同名库会莫名带色)。
- **凡是按库名存的东西,改名时都必须迁移**:配色、版本快照(`bankVersions`,改名走新增的
  `renameBankVersions()`)、错题/收藏的 `bankName` 归属,以及**编辑器自己开着的那个库名**
  (`state.editBankName`)—— 漏掉最后一项后果最重:旧键已不存在,`currentEditBank()` 返回空数组,
  看起来就像"改个名,这库的题全没了"(踩过)。清单化:动了 `questionBanks` 的键 ⇒
  立刻检查上面四项 + **重渲染编辑器**(标题、列表、版本面板一起按新名重画)。
- 入口:题库编辑面板 →「题库设置与管理」→「卡片配色」,**点一下即改即存**(配色是"看一眼就想调"的东西,
  不值得为它走一遍保存流程)。

## 3.5 题库编辑器

**结构 = 收矮的头部 + 两个标签页 + 唯一滚动主体**(它能在手机上用的前提):

```
.editor-head   库名(居中)                     ✕(绝对定位在右上)   ← 40px
.editor-tabs   [编辑题目 28] [题库设置]                            ← 44px
.editor-body   唯一滚动区,内容由 data-tab 决定:
   question → 工具行(sticky 悬浮)+ 筛选面板(悬浮卡片)+ 题目列表
       工具行:[⚙ 筛选(2)▾] [去重]   …圈选后→ 已选 2 题 [全选][取消][🗑 删除]
       筛选面板(两列):题型 | 缺什么 | AI | 标记 | 范围(只看勾选 N 题)
       列表每行:☑ | 序号 | 题干(一行截断) | 徽章(题型/⏳待补/⚠/🤖/✍️/🕘)
       列表末尾:[＋] ← 唯一的新增入口
       (工具行里**不写"共 N 题"**:标签页角标已经写着;筛选时角标变 19/28)
   编辑卡片(二级弹窗 1100,双击题目才跳出,见图下方)
   bank → 题库设置:按作用对象分块(配色 / 本库 / 错题本 / 版本记录 / ⚠ 危险操作)
```

```
编辑卡片 .edit-card(二级弹窗,压住编辑器):
  头部  ‹  第 3 / 12 题  ›                                    ✕
  主体  题干 / 题型与答案 / 选项 / 题目解释 / 解析 + 🤖 AI 补答案   ← 可滚动,全部展开
  底部                                          [取消] [保存本题]
```

**两个标签页的分界 = 作用域**:「编辑题目」只影响单题,「题库设置」影响整库。两类动作**绝不能同屏**
(旧版把「删除题库」和单题编辑混在一屏,是手机上最危险的一处)。

- 标签页显隐**只由 CSS 决定**(`.editor-body[data-tab=…]` + `.editor-tab:has(input:checked)`),JS 只写 `data-tab`。
  ⚠️ **`data-tab` 必须有默认值**:缺省时两页同时隐藏、编辑器变空壳(HTML 写死 + JS 兜一次)。
- 头部收矮到 **44px**;唯一按钮是关闭键(40px)。
- **滚动只发生在 `.editor-body`**(弹窗本身 `overflow: hidden`)—— 否则底部固定键会被滚走。
- **手机档**:弹窗整屏(`100%/100%`)、单列;列表**桌面与手机都常显**(没有"折叠起来"那套)。

### 题目列表:两条通道,各管一件事(👤 2026-09-12 定调,勿合并)

| 手势 | 结果 | 说明 |
|---|---|---|
| **单击题目(行)** | **高亮**(设为当前题) | 只换高亮,**绝不重画列表** —— 重画会换掉行节点,第二下落到新节点上,`dblclick` 永远不触发(踩过) |
| **双击题目** | **选中 → 翻开编辑卡片** | 一步改一道题 |
| **点复选框** | **圈选**(不改当前题、不掀卡片) | 勾选集有两个去处:筛选的「**范围 → 只看勾选 N 题**」、**批量删除**的目标 |

- 行 = `☑ + 序号 + 题干 + 徽章`;**序号用题库真实下标 +1**(不是行号),筛过之后仍对得上"这是第几题"。
- 行**不加任何左侧色条**(👤 2026-09-12:"题目条左边的蓝条…很丑";一排竖条像栅栏)。
  状态一律交给**徽章 + 底色**:待补/选项不足 = 淡黄底 + 徽章,当前题 = 淡主色底 + 加粗,AI 标记 = 徽章。
- **徽章 = 筛选面板的视觉词典**(同一套词):题型 / ⏳待补 / ⚠选项不足 / 缺解析 / 🤖AI 整理过 /
  ✍️AI 补的答案或解析 / 🕘历史标记。最多 4 个,其余进 `title`(否则一行变横幅)。
- **动作只在圈选之后出现**(批量栏);没圈选时这一行一个多余按钮都没有。
- 复选框**只认 `change`**(键盘/脚本也能走到);点它不许触发"单击高亮"或"双击开卡片"。

### 筛选:组内任一、组间同时

五组 —— `题型(单选/多选/判断)`、`缺什么(待补答案/缺解析/缺解释/选项不足)`、
`AI(AI 整理过/答案是 AI 补的/解析是 AI 补的)`、`标记(有历史标记)`、`范围(只看勾选 N 题)`。
空组 = 不约束。这样"待补 **或** 缺解析"(有问题的地方都给我看)与"题型:单选 **且** 待补"(交叉收窄)都能表达。

- 面板是**悬浮卡片**(点工具行的「筛选」展开,点面板外面收起),**不把列表往下挤**;桌面两列排组。
- ⚠️ 面板必须挂在 `.editor-toolbar-wrap` 里、**不能**放进 `.editor-toolbar` —— 手机档工具行要横向滚动,
  滚动容器会把绝对定位的面板**裁掉**(点不到)。
- **筛选一变就清空勾选**(安全底线:绝不让看不见的题留在待删集合里)。
  **例外**:切「范围 → 只看勾选」时**保留**勾选 —— 它就是这条条件的输入,清掉等于把条件清空。
- **「全选」= 选中当前筛选下看得见的题**(不是整个库):"筛出来 → 全选 → 处理"才是这条流水线的本意。
- **「新增题目」先清筛选**:新题很可能不满足当前条件,否则"加进去了却看不见",用户以为没加上。
- 筛没了的空态文案**必须区别于"库里没题"**:「没有符合筛选的题目 —— 换个条件,或点『清除筛选』」。
- ⚠️ 列表高亮/对号一律按 `data-idx`,**不许用 `children` 下标**(筛掉的行不占位,下标会整体前移 → 点亮错的题)。

### 批量删除 / 存版

- 二次确认;**删 ≥2 道时先自动存一版**(`批量删除前`);单删保留"确认即删"不占版本槽
  (每库只有 3 个槽 —— 与去重同一条口径:只在真会大改时存)。

### 编辑卡片

- 六个字段(题干 / 题型与答案 / 选项 / 题目解释 / 解析)**全部展开,不做折叠**;文案在**输入框左边**。
  「选项」区不带标题(选项行与增删键本身已自明)。判断题口径写 **A=正确 / B=错误**(存的就是 A/B)。
- 头部 `‹ 第 N/M 题 ›` **只在当前筛选下看得见的题之间走**,到边界**禁用**(不静默失败)。
- 保存成功即关闭;有未保存修改时关闭**先问一句**(复用 `editorGuard`)。
- ⚠️ 容器类名是 `.edit-card`,**不能**借用 `.question-card` —— 那是错题/收藏卡的类,自带 4px 红条,
  借来就会在卡片左边凭空长出一道红条(👤 反馈"很丑",已改)。

### 尺寸

- **触摸目标 ≥44px**(主原则):输入框 / 主要按钮 / 列表行 / 色块 / 标签。列表行整行 ≥44px、
  复选框手机 **24px**(桌面 18px)。
- **例外(👤 定调)**:列表控制条可以矮一档 —— 工具键/批量键 **24px 桌面 / 34px 手机**、
  筛选 chip **22 / 30**。而**编辑卡片底部的保存/取消仍守 44**(那是主要动作)。
- 设置页按钮 `.admin-btn`:描边圆角方框,桌面 40 / 手机 44,`min-width:150px`;桌面按内容宽**不撑满整行**,
  手机 `flex:1 1 140px` **填满整行**。危险键只描边不实心。
- 块里的说明文字用 `<p class="meta-note">` 时**要显式收掉默认上下边距**。

## 3.6 版本记录(库级快照)

**它是什么**:破坏性操作**之前**自动存的一份题库快照 —— **自动安全网,不是备份系统**。

| 项 | 规则 |
|---|---|
| 存版时机 | 覆盖导入前 / 去重前 / 恢复前。⚠️ **只在确认真的会改动题目之前存** —— "没有重复"的去重、"空库"的操作都不该占槽 |
| 保留策略 | 每库 **3** 版;全站 **10** 版,超出按时间 LRU 淘汰最旧 |
| 位置 | 编辑器 →**「题库设置」**标签页(与配色/重命名同属整库设置;**不在库卡上**) |
| 每条可做 | **恢复此版** / **删除** —— 只存不删是不完整的:存错了得能立刻清掉 |
| 恢复语义 | **可逆**:恢复前自动把当前内容存一份,再回退也能回来 |
| 空态 | 必须说明"哪些操作会自动存版",不要让用户以为它永远空的 |

⚠️ 实现注意:面板是**倒序渲染**的(新的在上),所以恢复/删除必须按**原数组下标**定位,
不能拿显示顺序当索引 —— 否则"删第 1 条"会删到错误的那条。
⚠️ 渲染时取一次库名并**守卫**:`editorClose()` 会把 `editBankName` 置 null,
关闭期间的重绘不该读一个已失效的库名(否则渲染出空壳,看起来像功能坏了)。

## 3.7 悬浮菜单(回收站)

> **点菜单外面收起**、再点触发器收起(👤 2026-09-12)。闭合态必须显式 `display:none`
> (本机 chromium 上闭合的 `<details>` 仍会渲染 children,见下),不能赌原生行为。

**模式**:触发器住在一行里(这里 =「我的题库」标题行的最右),点开在**触发器下方**弹出面板,
**不占文档流**(绝对定位),再点收起。

```html
<div class="recycle-menu">            <!-- position:relative, 浮层的锚点 -->
  <details id="recycle-bin" class="recycle-pop">
    <summary>🗑 回收站 <span id="recycle-count">(0)</span></summary>
    <div class="recycle-pop-panel">…</div>
  </details>
</div>
```

规则:
- **锚点与浮层成对**:外层 `.recycle-menu` 提供 `position: relative`,面板用 `absolute; top: calc(100% + 6px); right: 0`。
- **z-index 要够高**(这里 60)才压得住下方库卡。
- 面板自己 `max-height: 60vh` + `overflow-y: auto`,**不许撑高页面**。
- 宽度:`min(360px, 86vw)`,右对齐;手机档自动贴边,不缩成窄柱。
- ⚠️ **闭合态必须显式 `display:none`**(见 v17 踩坑):本机 chromium 上闭合的 `details` 仍会渲染 children,
  依赖原生行为会得到"没点开就已经看见了"。
- ⚠️ **计数要更新独立的 `<span>`**,不要整条重写 `summary.textContent` —— 那会把图标与结构一起冲掉。
- 一行内的对齐:`h3` 在左,动作按钮靠右(`#create-bank-btn { margin-left: auto }`),浮层紧跟其后。

## 3.8 浮层层级(改弹窗 z-index 前必读)

**层级表**:

| 层 | z-index | 谁 |
|---|---|---|
| 底部导航 | 100 | 手机端 nav |
| 刷题状态栏 | 90 | `.quiz-controls`(手机固定) |
| 悬浮菜单 | 60 | 回收站等"行内弹层" |
| **一级弹窗** | **1000** | 导入预览 / 题库编辑 |
| **二级弹窗** | **1100** | 重命名 / 创建题库 / AI 设置 —— **会被从一级弹窗里打开** |
| 答题卡抽屉 | 1200 / 1201 | 刷题页那一层,压在所有弹窗之上 |

- 🚨 **同级 z-index 时,谁在上面只看 DOM 顺序**。二级弹窗(`rename-bank-modal` 等)在 HTML 里排在
  `edit-bank-modal` **之前**,若都给 1000,从编辑器里打开就会被盖住 —— 表现是"对话框在编辑界面下面、
  点不到、改不了名"(👤 反馈的 bug)。**新增弹窗时先问一句:它会不会从另一个弹窗里打开?会 → 给 1100。**
- 答题卡抽屉必须高于二级弹窗(它在刷题页最上层)。
- **浮层不占文档流**:回收站那类悬浮菜单用 `position:absolute` + `[open]` 控制显隐。
- ⚠️ 改 z-index 后**不要只看截图**:同级覆盖问题在静态图上完全看不出来,要断言
  `elementFromPoint(弹窗中心)` 落在该弹窗内(见 `tests/dom-structure.test.js` 的守卫)。

## 3.9 题库文件格式(导出 / 导入往返)

**导出「导出题库」= 一个文件,逐库一段**,每段第一行是分节标题:

```
# ===== 题库：高数期末 =====

题目：…
答案：A
…

# ===== 题库：英语期末 =====
```

- ⚠️ 标题字符串只有**一个来源**(`bankSectionHeader()`),导入侧用 `splitBankSections()` 认它 ——
  两边各写各的字符串,迟早一边改了另一边没改,那时导出的文件就导不回来。
- ⚠️ 这个标记必须**先于** `TITLE_RE` 判定。否则 `# ===== 题库：甲 =====` 会被当成"一道题的标题",
  **所有库的题混进同一库**(👤 反馈的 bug 根因)。兼容老写法 `# 题库：甲` 与全角冒号。
- 单个库导出(`导出本库`)没有标记 → 导入走原来的单库流程(行为不变)。

**文件名带时间戳**(👤 2026-09-12):`所有题库-20260912-1523.txt` / `甲库-20260912-1523.txt` ——
纯数字 + 连字符(各系统都不挑,也不含冒号这种非法字符),按本地时间。理由:`所有题库.txt` 一天导两次,
浏览器只给 `(1)(2)` 这种含糊后缀,过几天分不清哪份是哪份。

**导入:文件里有 ≥2 段 → 自动认出多题库**
- 顶部横幅:「检测到 N 个题库:甲(12 题) · 乙(8 题)」+ 导入方式单选:
  **📚 按题库分别导入(默认)** / 全部并入一个题库。
- 「导入到」下拉在"分开导入"时**隐藏**(没有意义);「覆盖」文案随之变「覆盖同名题库(清空后导入)」。
- 预览里**每道题都带库名徽章**(📚 名称)—— 用户能核对切分对不对,而不是盲信。
- 分开导入的纪律:**同名库追加**;勾了「覆盖」才清空重建,且**逐库存版**(可回退);
  **逐库记一条导入批次** —— 撤销才撤得掉,而且只撤那一个库,不牵连别的库。

## 3.10 PWA:manifest 与图标(👤 2026-09-12 提到 P0)

**目的**:Zquiz 能"装到主屏"、带自己的图标、点开**全屏无地址栏**。
⚠️ **本批不含离线缓存(service worker)** —— 👤 已取消,另行设计;`sw.js` 出现即视为越界(有守卫)。

```
icons/icon.svg / maskable.svg   ← 图源(入库,以后改图标只动这两个 + 重渲)
icons/icon-192.png  icon-512.png  maskable-512.png  apple-touch-icon-180.png  favicon-32.png
manifest.webmanifest            ← name / short_name / display:standalone / theme_color / icons
index.html head                 ← manifest + icon + apple-touch-icon + iOS meta + theme-color 亮暗两档
```

**图标规范**(👤 2026-09-13 重设计:**Z 元素 + 刷题元素 + 书本元素,简洁**)
- 构图 = **蓝底 + 白色摊开的书 + 书页上的蓝色 Z + 右上角绿色对勾徽章**。三个元素说清三件事:
  书本 = 题库,Z = 品牌,✓ = 刷题(答对)。三色(主色蓝 / 白 / 绿),**不写字、不加阴影、不做立体** —— 48px 下必须还认得出。
- 几何(512 画布):圆角方底 radius **112**(22%);书 = 两页四边形 `M112,290 L248,266 L248,414 L112,438 Z` 与镜像,
  两页之间留 **16px 缝**(缝里露出底色 = 书脊,不画线也认得出是书);Z 写在书页上(两横 108×30 + 一斜)居中;
  对勾徽章 `cx380 cy158 r60` + **12px 底色蓝环**(镂空圈,把徽章从底上"抠"出来)。
- 配色:底 = 主色渐变 `#3b82f6 → #1d4ed8`;书页 `#fff`;**Z = `#1d4ed8`**(蓝字压白页,对比足);
  徽章 = **深绿 `#12a150`**(不用浅绿:白勾压在浅绿上对比不够)。
- 安全区:**maskable 版把整组内容再缩 88%**,保证前景(书页 + 徽章,**不含底色**)全部留在中央 80% 圈(半径 205)内
  —— 有守卫逐像素扫描"安全圈外不得有任何前景像素"。
- 两套底:常规图标**圆角外透明**;`maskable` 与 `apple-touch-icon` **必须不透明且铺满**
  (iOS 会把透明底填成黑/白块;安卓 maskable 自己裁形)。守卫会逐像素验这三件事。
- 零依赖 → PNG 用**无头浏览器把 SVG 渲成图**(`scripts/probe.sh` 同一套路),不引任何图形库;
  图源只有 `icons/icon.svg` 与 `icons/maskable.svg`,改完重渲五张 PNG 即可。

**manifest 规则**
- `start_url` 与 `scope` **必须是相对路径**(`./`):站点在 `/zquiz/` 子目录下,写 `/` 或绝对 URL 会 scope 错位 →
  **装到主屏后点开白屏**(最难一眼看出的坑)。
- 图标至少 **192 + 512**(浏览器安装门槛)+ 一张 `purpose: maskable`。
- `theme_color` **取页面底色**(亮 `#f5f7fa` / 暗 `#121417`),不取品牌蓝 —— 浏览器地址栏与状态栏因此与页面无缝,
  在浏览器里的观感与改动前完全一致;蓝色只留给图标本身。
- iOS 不读 manifest 的部分能力:靠 `apple-mobile-web-app-capable` / `-title` / `-status-bar-style` 与
  `apple-touch-icon`;iOS 也**没有 `beforeinstallprompt`**,所以"引导"只能是一句文案,不做弹窗。

## 4. 字号阶梯(唯一可选值)

`12 / 14 / 16 / 20 / 24` px,行高:正文 1.5~1.7,标题 1.2。

- 页面标题 20 · 卡片标题 16 · 正文/输入 16(**表单输入一律 16,防 iOS 聚焦缩放**)· 辅助 14 · 角注 12
- 12.5/13.5/15/19/22 等都是**非法值**,见到即改。
- 层级靠**字号+字重**(600/700),禁止靠加颜色制造层级。

## 5. 间距刻度(8px 系,4px 只作紧凑特例)

`4 / 8 / 12 / 16 / 24 / 32` px。禁止 6/7/10/14/15/18/20/30 等野值(存量清理中,新代码严禁)。

- 组件内边距:紧凑 8 · 常规 12 · 宽松 16
- 组件间距:同组 8 · 异组 16 · 分区 24
- 手机(≤768px)一律取小档。
- **内容区内边距走令牌,三页共用一套**(👤 要求"以后统一"):
  `--pad-x`(左右留白)`--pad-top`(标题栏与首模块之间的呼吸位)`--pad-bottom`。
  `main` 只写 `padding: var(--pad-top) var(--pad-x) var(--pad-bottom)`;手机端
  **只覆盖 `:root` 里的三个令牌值**(12/12/24),不再另写一套 `main` padding。
  改内边距只需改这两处,不会再有散落的写死值。
- ⚠️ **`--pad-top` 必须大于 0**。曾把它设成 0,导致刷题/题库页的首个模块直接贴住标题栏
  (👤 反馈"直接没了空隙")。三页**统一**同一个值,不搞"首页紧、其他页松"的差异。
- 反面教材:一度加过 `.section.active > *:first-child { margin-top: 0 }`,
  它会让"首个元素是卡片/是标题"的分区呈现不同顶部效果,已删除 —— **统一优先于逐页微调**。
- ⚠️ **顶部间距只由 `--pad-top` 一处控制**:任何模块都**不得**再写 `margin-top` 去加"自己的上方留白"。
  首页导入卡曾有一条 `#home-section .operation-card { margin-top: 24px }`,叠加在 `--pad-top` 之上,
  就成了"卡片上方多一块空白"。**要更紧/更松,只改 `--pad-top`。**

## 6. 圆角(两档)

- **12px**(`rounded-xl`):卡片、模态框、嵌入面板(operation-card / quiz-container / modal-content / rescue-box)
- **6px**(`rounded-md`):按钮、输入框、行内控件、徽章
- **999px**(胶囊):主题开关、状态 chip、连对徽标

嵌套圆角:子元素 ≤ 父元素。

## 7. 边框与阴影(降噪)

- 默认**无边框**:用背景色差(`--c-surface` vs `--c-surface-alt`)和间距分区。
- 1px 边框只用于:输入框、需要精确边界的行内控件。
- 阴影只用于:悬浮层(模态框/底部操作条/导航),卡片静态态不加。
- 左侧 3px 色条 = 语义标记专用(见 §3),不作装饰。

## 8. 触控与移动端(≤768px)

- 可点目标 ≥ 40×40px(含 padding)。
- 表单输入 `font-size: 16px`(防 iOS 聚焦缩放)。
- 底部固定层顺序:导航栏(最底) → 刷题操作条(其上);`env(safe-area-inset-bottom)` 必须参与计算。
- 固定层下方的内容必须预留等高 padding,禁止被遮挡(教训:选项 D 被操作条盖住)。

## 8.1 宽度陷阱:flex 子项上的 auto 边距(🚨 必读)

**桌面居中常用的 `margin-left: auto; margin-right: auto`(配 `max-width`)在元素变成 flex 子项后是危险的** —
flex 布局里 auto 横向边距会吸收**全部**剩余空间,于是元素不但不按 `flex` 拉伸,还会被压到**最小内容宽度**。

事故现场(2026-09-11):`.quiz-container` 用 auto 边距居中(阅读档 640px),手机媒体查询把它改成
`flex: 1` 的 flex 子项 → 390px 手机上容器只剩 **82px**,题干与选项全成一条窄柱,
看起来像"题干短所以窄",实际**每一题都窄**(桌面不受影响,因为桌面它不是 flex 子项)。

规则:
- 任何元素一旦成为 flex/grid 子项,**必须清掉继承来的 auto 横向边距**(显式 `margin-left/right: 0`),
  宽度交给 flex 拉伸决定。**光写 `flex: 1` 是不够的** —— 两者要出现在同一条规则里。
- `tests/dom-structure.test.js` 有守卫:手机档的 `#quiz-container` / `#quiz-result` 规则必须含
  `margin-left/right: 0`。新增"桌面居中 + 手机 flex"的模块时,**照抄这条**。
- 同类现状:`.quiz-settings` / `#home-section > .operation-card` / `#banks-section > *` 也用了 auto 居中,
  它们的父容器目前不是 flex,故暂时安全;一旦父级改成 flex,**必须同步清零**。

## 9. 空状态

每个列表必须写空提示:一句话说明 + 下一步动作(如"暂无收藏题目,刷题时点 ☆ 收藏即可加入")。禁止留白屏。

## 10. 组件速查

| 组件 | 规格 |
|---|---|
| 主按钮 `.action-btn` | `--c-primary` 底/白字/6px 圆角/padding 9×14(手机) |
| 次按钮 `.secondary` | `--c-text-muted` 底白字;或白底描边(需弱化时) |
| 状态条 `.status-line` | 常驻不自动消失;绿(结果)/黄(注意)/红(错误)/灰(中性) |
| 导航(手机) | 底部固定 5 tab,图标+11px 文字,active 蓝字无底条 |
| 预览项 `.preview-item` | 无边框,`--c-surface-alt` 底,左条=语义标记 |
| 选项卡片(刷题) | 整行可点 6px 圆角;选中蓝边;判分后锁定 |

## 11. Do / Don't

- ✅ 改样式先查本文件与 `:root`;❌ 裸写 hex/野值间距/混合圆角。
- ✅ 新功能先想空状态;❌ 只做有数据的 happy path。
- ✅ 多色仅用于语义标记;❌ 用第二颜色做"重要"。
- ✅ 手机优先验证(375px);❌ 只在桌面看效果就提交。

## 12. 交互规范(源自 Vercel web-interface-guidelines 的适配精选)

**必做(已在代码中固化):**
- 所有可点元素 `touch-action: manipulation` + 透明 tap-highlight(去双击缩放延迟)
- `:focus-visible` 焦点环(键盘可见,鼠标不扰)
- 弹窗 `overscroll-behavior: contain`(滚动不穿透)
- `prefers-reduced-motion: reduce` 时动效归零
- 加载中按钮:禁用 + 文案变化(如「🤖 整理中…(点击取消)」)
- 破坏性操作必须 confirm(删除/覆盖已有)
- 状态不只靠颜色:★/☆、⏳⚠🤖🕘 图标与文字并列
- 表单输入 16px 起步;不禁止缩放;不阻止粘贴
- 空状态必有"一句话 + 下一步动作"

**写新交互时的检查项:**
- 图标按钮必须有 title/aria-label;图标必须有文字标签
- 按钮语义用 `<button>`,跳转才用 `<a>`
- 破坏性按钮(删除)与普通动作拉开距离(错题本 action 行右置)
- 长任务必须有:进度反馈、可取消、完成后明确结果
- 错误信息要给出路(如"AI 失败:…可改用 A 路线")

## 13. 宽度系统(按钮与卡片)

**容器四档 max-width(已全部令牌化,唯一来源 = `:root`):**
| 档 | 令牌 | 宽度 | 用途 |
|---|---|---|---|
| 表单/弹窗 sm | `--modal-sm` | 500px | 确认/命名/设置类模态框 |
| 阅读 | `--reading-width` | **640px** | 刷题配置区、刷题容器、结果页、**首页与题库页的最外层模块** |
| 弹窗 lg | `--modal-lg` | 760px | 导入预览/题库编辑 |
| 页面 | `--page-width` | 1000px | 全局 `.container` |

⚠️ **不得在别处写死 `max-width` 像素** —— 必须引用档位令牌。
`tests/dom-structure.test.js` 有守卫(剔除媒体查询条件后,任何写死值都会失败)。
反例:首页卡片曾没有 max-width,撑满 1000px 页面档,而刷题/结果页走 640px 阅读档,
于是"首页明显比后俩页宽"(👤 反馈)。**同宽是三页一致性的第一条**,靠档位保证,不靠逐页调。

**按钮宽度三语义(同组内必须一致):**
| 语义 | 规则 | 用途 |
|---|---|---|
| inline(默认) | 自然宽度 | 工具条、并排次要动作 |
| block | 整行(`width:100%`) | 表单唯一主操作(如撤销卡、粘贴动作在窄栏) |
| flex:1 | 组内均分 | 固定操作条(刷题/撤销双钮) |

**栏比例**:题库管理 = 列表 1 : 操作 1(等宽;2:3 实测失调已回退);编辑器 = 列表 220px 固定 + 表单 1fr。
手机(≤768px)一律解除限宽,100% 全出血。
