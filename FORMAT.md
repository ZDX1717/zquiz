# Zquiz 题库格式详解

> `README.md` 只讲重点;这里放**完整格式说明、字段清单、文件导入能到什么程度**,以及可照抄的示例。
> 结论先行:**不用事先整理格式** —— 把题目原样粘进导入框,解析器会自己认。

## 一、怎么把题弄进来

| 入口 | 支持什么 | 说明 |
|---|---|---|
| 粘贴文字 | 任何文本 | **最稳**。Word / 网页 / 微信 / PDF 里选中的文字都行,按段落与表格结构读取 |
| 选文件 | `.txt` `.md` `.csv` | 当纯文本读,格式按 §二 那几种写法;自动识别编码:**UTF-8 与 GBK / GB18030**(txt 多是 GBK,认得出就不乱码) |
| | `.docx` | 本地零依赖解压抽取正文 |
| | `.doc` | 老版二进制格式读不了 → 提示「另存为 .docx 再选」或直接复制文字 |
| | `.pdf` | **文字版**能抽出文字(见 §六);扫描件、字体没带映射、加密、叠水印的会明确说读不出,不硬塞 |

- 文件读出来的文字**先落进输入框**(可编辑、可删改),再点「解析并预览」;不走第二条管道。
- 认不出题目时:点「🤖 AI 整理输入框」把原文理顺,或复制提示词交给聊天 AI 整理(两者都不改内容、不编答案)。

## 二、支持的题目形态

| 格式 | 样子 | 常见来源 |
|---|---|---|
| 字段式 | `题目：` `A：` `答案：` | 手工整理的题库文件 |
| 编号式 | `1. 题干` / `A. 选项` / `答案：B` | 题库软件导出、Word 文档 |
| 单行混排 | `1. 题干 A.xx B.xx C.xx D.xx 答案：B` | 网页 / 微信复制 |
| 判断题 | 答案写 `对/错/√/×/正确/错误` | 各类题库、判卷表 |

**分隔符都能认**:`题目：` / `题目:` / `题目 ` 等价;选项 `A：` `A.` `A、` `A)` 等价;答案大小写不敏感,
多选写 `CA`、`A、C`、`ABD` 都能正确判分。**题干很长没关系**(案例题 / 材料题:每个小题单独编号即可,
不必把材料拆开写)。

## 三、答案怎么写

- **行内 / 整行**:`答案：B`;多选连写 `答案：ABD`;判断 `答案：对` / `答案：错`
  (判断题会自动配好「A 正确 / B 错误」两个选项,不必自己写选项)
- **题末括号内嵌**:`……构成故意杀人罪。（B）`、`（ABD）`、`（x ）`
- **文末集中答案表**:按题号回填,支持下面几种写法
  ```
  参考答案：
  1.B
  2.【A】
  3-5 BBA
  ```
  也支持单行多对(`答案：1.B 2.A 3.D`)与对错判卷表(`1.对 2.错`)。
  ⚠️ 两个前提:**答案区必须在文档末尾**;**题号要与题目编号对应**。
- **判断语境的"只标错"惯例**:同一批材料里若存在 `（x）`/`（√）` 判卷痕迹,
  未标记的空括号题自动视为**对**(判卷习惯:只标错的);带选项的选择题不受影响。
- **没有答案**:预览里标「缺答案」,确认导入时会明确提示"N 题因缺答案未导入"(不会静默丢弃)。
  括号里是 `?` 之类的非答案字符时,如实保留为缺答案。

## 四、可选字段(字段式)

```
# 题目标题（可选）
题目：题目内容
A：选项A内容
B：选项B内容
C：选项C内容
D：选项D内容
答案：A
题目解释：这道题在问什么（作答时可见）
A解释：选项A为什么对/错（逐题模式下显示在选项下方）
解析：答案的详细解释
类型：单选
```

- `题目解释：` 与 `解析：` 是两件事:前者解释题干(作答时可见),后者解释答案(判分后可见)
- `类型：单选/多选/判断` **可省略** —— 系统按答案自动判断;但**类型与答案冲突时以答案为准**
  (例:写了「单选」却给了 `答案：AC`,会自动纠正为多选并提示)

## 五、容错能力(真实文档里常见的脏格式)

- **选项布局**:双行选项(第二行接着 `C.xx D.yy`)、全/半角逗号与顿号分隔(`C,xx D，yy`)、
  空格分隔(`A 盗窃罪`)、选项折行自动归并(不会把选项混进题干)
- **噪音清理**:空答题槽括号 `（）` 自动清除;文首标题行(如 `# 单选题1`)不当成题目
- **判分容错**:答案与选项的大小写、分隔符写法差异不影响判分
- **查重**:按「题干 + 选项」指纹,同一批内与目标题库内自动跳过重复题
- **缺答案**:进"待补答案"池,刷题时自动排除,补完答案即可转正

## 六、PDF:能读到什么程度

- **文字版 PDF**(讲义 / 题库导出的 PDF):选文件即把文字抽进输入框 —— 自己扫对象与内容流,
  按页面上**真正的位置**还原行序,不依赖任何第三方库。
- 下面四种**不硬导入**,会说明原因 + 给替代路,绝不把交错 / 乱码的文字倒进输入框:

| 情况 | 提示 | 替代路 |
|---|---|---|
| 扫描件 / 图片版 | 这是图片,读不出文字 | 把文件发给能看图的 AI 转录;或用系统「提取文字」 |
| 字体没带文字映射 | 字体缺映射,抽出来必是乱码 | 同上;或找一份可复制的版本 |
| 有密码 | 有密码保护(常见只是「权限密码」,打开并不需要密码) | 打开后另存一份不加密的副本再选 |
| 叠着水印 / 另一层文字 | 正文与水印织在一起,分不开 | 换一份干净源文件;或用「🤖 AI 整理输入框」按题号理顺 |

- **图片、公式、表格语义**目前不解析,也**还没有**"原文里有图 / 有公式"的提示 —— 相关能力在长期任务里持续补。

## 七、多题库文件(导出 / 再导入)

「导出题库」按库分段,每段开头一行标记:

```
# ===== 题库：行测 =====
1. 题干…
答案：B

# ===== 题库：申论 =====
1. 题干…
答案：A
```

- 导入这份文件时**自动认出多个题库**,默认**分开导入**;也可以选「全部并入一个题库」。
- 标记写法兼容 `# 题库：甲` / `## 题库:甲` / `===== 题库：甲 =====`(手写也行);
  ⚠️ 它必须**单独一行**,否则会被当成题目标题。
- 预览里每道题带**库名徽章**,能核对切分对不对;同名库默认**追加**,勾「覆盖」才清空重建。

## 八、完整示例

### 多选题

```
# 多选题示例
题目：以下哪些是编程语言？
A：Python
B：Java
C：C++
D：HTML
答案：ABC
解析：Python、Java和C++都是编程语言，HTML是标记语言
```

### 带解释与解析的单选(全字段)

```
# 单选题1
题目："祸兮福之所倚，福兮祸之所伏"体现了（）
题目解释：灾祸中潜藏着福气的可能，福气里也隐含着灾祸的隐患。
A：矛盾的同一性
A解释：矛盾双方相互依存、相互贯通，能够在一定条件下相互转化。
B：矛盾的斗争性
B解释：矛盾双方相互排斥、相互对立的属性。
C：矛盾的特殊性
C解释：不同事物的矛盾具有各自独特的特点。
D：矛盾的普遍性
D解释：矛盾存在于一切事物中，贯穿事物发展全过程。
答案：A
解析：题干中福与祸相互依存、相互转化，完全契合矛盾同一性的定义。
类型：单选
```

### 编号式(最常见的"复制粘贴"形态)

```
1. 我国现行宪法是哪一年通过的？
A. 1954年
B. 1975年
C. 1978年
D. 1982年
答案：D

2. 下列属于行政处罚种类的有（多选）
A. 警告
B. 罚款
C. 拘役
D. 吊销许可证
答案：ABD
```

### 判断题 + 文末答案表

```
1. 宪法是国家的根本法。（ ）
2. 行政处罚只能由行政机关实施。（ ）
3. 民事责任与行政责任可以并存。（ ）

参考答案：
1.√
2.×
3.对
```
