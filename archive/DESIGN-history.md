# DESIGN 修订历史(v1~v49 归档)

> 从 `zquiz/DESIGN.md` §0 迁出(👤 2026-09-12:"设计规范太冗长")。
> 正文里仍然有效的**规则**都已并进 DESIGN.md 的对应章节;这里只保留"当时改了什么、为什么"的过程记录。

- v21(2026-09-12):**题库设置重新排版**(👤 要求"重新规划排版,按钮形状大小可自行更改")。旧的"三行混排按钮"改成**按作用对象分块**:卡片配色 / 本库 / 全部题库 / 错题本 / 版本记录 / 危险操作;每块一个小标题,标题即目录。要点:① **「删除题库」独占最底部的危险区**(淡红底 + 红标题 + 红描边按钮),不再和重命名/导出同排 —— 这是"两类动作不同屏"的延伸;② 设置页按钮改成自己的一套 `.admin-btn`(描边圆角方框,桌面 40px / 手机 44px,`min-width:150px`),桌面按内容宽**不撑满整行**,手机 `flex:1 1 140px` **填满整行**;③ 版本记录改用宿主容器 `#editor-versions-host` 挂载(JS 只有 `appendChild`,直接挂容器末尾会跑到危险区下面);④ 踩坑两条:块元素用 `<div>` 而不是 `<section>`(守卫按第一个 `</section>` 切片)、`<p class="meta-note">` 要显式收默认边距;⑤ 守卫别用关键词 `indexOf` 定位置 —— 说明性注释里也会提到「危险操作」,会搜到注释的位置。

- v20(2026-09-12):**删掉「当前题库:xxx」+ 修改名不刷新**(👤 反馈)。① 「题库设置」页顶部那行「当前题库:xxx」删除 —— 库名在编辑器标题栏已经写着,同一屏再挂一遍是噪音(它也正是"改名后屏幕上还留着旧名"的现场)。② bug:在编辑器里重命名后,标题/列表/版本记录仍是旧名。根因是 `renameBank()` 只挪了 `questionBanks` 的键,没改 `state.editBankName` —— 旧键已不存在,`currentEditBank()` 直接返回空数组,表现为**「改个名,这库的题全没了」**;同时按库名存的**版本快照**也要一起迁移,否则面板显示"暂无记录"而历史成孤儿(新增 `renameBankVersions()`)。

- v19(2026-09-12):**「保存」跟着选中的那一道走**(👤 要求)。动作行里的 `#editor-save-btn`「保存本题」删除;题号列表的每一行改为 **✕(左) → 正文 → 保存(右)**,两颗按钮**只对选中那一行现身**。关键实现:非选中行用 **`visibility:hidden` 占位**而不是不渲染 —— 行宽恒定,换题时文字不重排、按钮也不会在手指底下挪窝(点第 3 题再点第 4 题时,✕ 位置不动)。同时修掉一个暗病:列表选中态原按 `children` 下标对号,「只看待补」筛掉行后会**错行点亮**,现改按 `data-idx` 对号。

- v18(2026-09-12):**修两个 bug**(👤 反馈)。① 从编辑器里点「重命名」,对话框被编辑器盖住、无法操作 —— 根因是所有 `.modal` 同为 `z-index:1000`,**同级只看 DOM 顺序**,而二级弹窗在 HTML 里都排在编辑器之前;改为**二级弹窗 1100 > 一级 1000**。② 删库后回收站要刷页面才更新 —— `deleteBank()` 漏调 `renderRecycleBin()`。新增 §3.8。

- v17(2026-09-12):**回收站改为悬浮菜单**(👤 要求):从题库页底部的折叠块移到「我的题库」标题行的**最右**(紧跟「＋ 创建题库」),点开是**绝对定位的悬浮面板**,不占文档流。踩坑:闭合的 `<details>` 在本机 chromium 上**仍会渲染 children**(实测 `open=false` 却有 44px 高、文字可读),故闭合态必须用 `.recycle-pop:not([open]) > .recycle-pop-panel { display:none }` **显式隐藏**,不能赌原生默认行为。

- v16(2026-09-11):**版本记录理顺 + 迁入题库设置**(👤 要求)。① 补上**删除单条版本**的能力(原来只能存、不能删,存错了只能等它被淘汰);② 修正存版时机 —— **只在确认真的会改动题目之前存**(去重先查有没有重复,没有就不存,否则空点一次就白占一个槽;每库只有 3 个槽);③ 版本区从库卡手风琴**移到编辑器的「题库设置」**标签页(它属于整库设置,和配色/重命名同类);④ 空态给文案说明"哪些操作会自动存版"。新增 §3.6。

- v15(2026-09-11):**表单文案与判断题口径**(👤 定调)。① 字段文案一律放到**输入框左边**(固定宽标签列 + 自适应字段,`display:flex` 实现,窄屏整体换行不横向滚动);② 删除「选项」标题(选项行与增删按钮本身已自明);③ 判断题提示改为 **A=正确、B=错误**,不再让用户填「对/错」(字段存的就是 A/B;填「对/错」仍被接受并归一,见 `editorSaveCurrent`)。

- v14(2026-09-11):**编辑器再收敛**(👤 定调)。① 标题栏只剩"库名居中 + 右上角关闭"(`position:absolute` 让库名**真正居中** —— flex 左右等宽做不到,因为关闭键只有一个);② **取消底栏**,「保存本题 / 新增题目 / 只看待补答案」三个常用动作移到**题目列表上方**的动作行;③ **取消「上一题 / 第 N 题 / 下一题 / 删除本题」**——题号列表常显且每行自带删除,列表本身就是导航;④ **题干 / 题型答案 / 选项 / 解释 / 解析合成一个表单区块,取消全部折叠**(原「题目解释/解析」的 details 移除);⑤ 题号列表**不再收起**(上一版的开合标题删除)。踩坑:我一度把「关闭」当成"底栏那颗按钮"整条删掉,其实只是移到标题栏 —— 功能必须留着。

- v13(2026-09-11):**编辑器改为两标签页 + 头部收矮**(👤 要求)。①「编辑题目」(题号列表 + 当前题) 与「题库设置」(整库) 拆成两个标签页,由 `data-tab` + CSS `:has(input:checked)` 表达;② 头部只留 关闭 / 库名 / 进度,整条压到 **44px**(标签条同高),动作全部下沉到标签页;③ 题号列表**每一项都带删除键**(👤 要求"按钮可以新增和删减"),带二次确认,手机触达 44px。踩坑:首屏若 `data-tab` 缺省会让两页**同时隐藏**(实测),故 HTML 给默认值 + JS 双保险。

- v12(2026-09-11):**题库编辑器重构(手机优先)**(👤 要求"易于触摸、逻辑合理、分区清晰")。原文见 §3.5:三段式(头部进度 / 唯一滚动主体 / **底部固定操作栏**);手机整屏、单列、**题号列表默认收起**由头部按钮拉出(选完自动收起)、所有可点元素 **≥44px**、输入框字号 ≥16px(防 iOS 聚焦缩放);**题目级操作与题库级操作彻底分开** —— 「删除本题」移出底部动作栏、跟着题号导航走,题库设置收进折叠区。顺带修了"只看待补时进度显示 3/3(实际只有 1 条)"的 bug。

- v11(2026-09-11):**题库卡改为"配色底 + 框"**(👤 定稿):去掉左侧 4px 主色装饰条,默认 = 白底 + 普通边框(与站内其它卡片一致),可选 5 色(蓝/绿/红/琥珀/青)。配色走 `data-color` + `:root` 成对令牌 `--c-bank-<名字>` / `--c-bank-<名字>-border`,**暗色另配一套同色相深值**(浅底直接套用会刺眼)。卡片内的底部按钮改半透明白底以适配任意底色。入口在题库编辑面板的「题库设置与管理」区,点一下即改即存。新增 §3.3 卡片配色小节。

- v10(2026-09-11):**题目卡片统一**(👤 要求):错题卡与收藏卡合并为一套 `.question-card` 结构 —— 题型 / 收藏 / 删除 一律移到**题干上方一行**(`.question-card-tags`,nowrap + 靠右动作,尺寸复用 `--tag-row-h`);收藏卡从"答案直接摊开"改为与错题卡相同的 `details` 主动回忆遮挡;删除键由实心红改为描边红(令牌 `--c-danger-hover` 随之删除)。**新增 7 条测试**(`tests/question-card.test.js`)+ 桩升级(`appendChild` 记录子节点,否则结构类断言无法进行)。标出 `errorbook.js`/`favorites.js` 内的旧卡片渲染为死路径。

- v9(2026-09-11):**题干上方标签行收口**(👤 要求「统一高度 + 减少上面空白」):三件(答题卡/题型/收藏)高度改由**单一令牌 `--tag-row-h`** 统一(基准 28px、手机 26px),字号一律 12px —— 原先各写各的,实测 28/20/22 高度 + 13/12/12 字号,一行高低参差。顶部空白:题干区到标题栏由 **64px 收到 32px**(`.quiz-container` 的 `padding-top` 24→16、`margin-top` 24→8;两者原本叠加)。**新增 2 条守卫**(等高、尺寸下限)。本次**无新增令牌**(复用既有变量机制,新增 `--tag-row-h`)。

- v8(2026-09-11):**修 bug —— 手机上题干与选项被压成窄柱**(👤 反馈"题干只有几个字时很窄很突兀")。真因与题干长短无关:`.quiz-container` 桌面用 `margin-left/right: auto` 居中,手机档把它变成 **flex 子项**后,flex 子项的 auto 横向边距会吃掉全部剩余空间、把元素压到最小内容宽 —— Chromium 实测 390px 手机上容器只有 **82px**(修复后 366px,与题干长短无关)。修法:手机档显式 `margin-left/right: 0`(题干容器与结果页都要)。**新增守卫**:手机档必须存在同时含 `display:flex` 与 `margin-left/right:0` 的容器规则。本次**无新增令牌**。

- v7(2026-09-11):**修 bug —— 「确认答案」变文案把状态栏挤高、按钮上下错位**(👤 反馈)。根治两手:①`#next-question-btn` 预留固定宽度(`min-width: 76px` + 文字居中),让两种文案**等宽**,从源头消除"按钮宽度会变";②进度/答题卡 + 题型 + 收藏整体移到题干上方标签行(`.question-tags`,一行三件、从左到右、`nowrap` + 子项 `flex:0 0 auto`),状态栏左区只剩「智能切题」。**新增铁律:标签行按钮的宽窄变化不得影响题干宽度;左区不得再放任何会随内容变宽的按钮**。本次**无新增令牌**。

- v6(2026-09-11):**修 bug —— 套题模式答题卡交卷前泄露对错**(👤 反馈)。改为四态:新增中性的 `picked`(套题交卷前"已选未判分"),`correct`/`wrong` 只在 `finishExam()` 置真的 `q_examGraded` 为真时出现;概览行交卷前只报"已选/未答"。判分台账清零收敛到新的 `resetGradingState()`(修掉"新一局继承上一局对错"的隐患)。本次**无新增令牌**。

- v5(2026-09-11):**刷题状态栏定稿**(👤 定调):①收藏按钮移出状态栏,改住「题型徽章」之后、题干之前(`.question-tags` 一行);②状态行重组为 `答题卡入口 + 智能切题` 一行(两行结构 `quiz-meta-row` 取消);③三处按钮**放大到中号**(`min-height` 28px / 13px 字,手机 26px / 12px),判据 = 先保证点得准、看得出能点,再谈排一行。本次**无新增令牌**。§3.2 相应重写。

- v4(2026-09-11):新增 **§3.2 答题卡抽屉规范**(P0-6.1):入口 = 刷题状态栏的进度本身、底部弹出、一格一题 + 单/多/判题型角标、三态"颜色 + 记号"双通道(未答用虚线框)、当前题用 `outline` 不覆盖三态底色、**抽屉内不得出现题干与答案**、判分事实只认 `q_graded`(勾了没确认 = 未答)。本次**无新增令牌**(三态复用 `--c-success*`/`--c-danger*`/`--c-border`/`--c-primary`)。

- v3(2026-09-10):**令牌表全量重校准**——v2 之后配色经历"去紫换真宝蓝"(blue-600/暗 blue-500),但本文件未同步,导致主色记载为 `#4a6cf7`(紫系)而 `:root` 实为 `#2563eb`。本次按 `:root` 与 `html[data-theme="dark"]` 实测值**逐条对齐**,并补齐 v2 起新增的全部令牌。同时**收口 30 处裸 hex**(§2.1),亮色零视觉变化、暗色首次可换肤。**并按 👤 拍板合并 `--c-primary-alt` 别名、删除零引用令牌**(令牌数 66 → 63)。**新增 §3.1 三页面职责边界**,并据此把首页宣传块整体删除、把"复习"改为刷题页的题源维度。**新增 §3.2 刷题两屏规范**(设置页三级结构+分段控件,完成页"一行大字 + 一行 chips")与 **§3.3 标题栏与错题条目规范**(标题栏收窄为常驻应用栏并承载主题开关、错题条目题型前置且选项裸露但不泄露错选、答案展示统一走 `formatAnswerForDisplay`)。

- v2(2026-09-10):采纳 slate/emerald/rose/amber 色阶与"卡片 12px+细边框+微阴影"体系(👤 提供的外部规范);按钮/输入保持 6px。v1 的 6px 唯一圆角规则废止。

- **v49(2026-09-14)** 删掉「AI 整理成标准格式」**救援区**(👤 定),两个按钮各自归位:
  ① 「🤖 AI 格式整理并填入输入框」→ 改名 **「🤖 AI 整理输入框」**、挪到**下排挨着「选择文件」**
  (两条路都是"对输入框下手",并排才说得通;旧名字把"怎么用"写进了名字里);
  ② 「📋 复制提示词和题目」+ 说明 → 收进**按需提示区**(`#ai-fallback`,状态行下方):
  **平时占 0 高度**,只有解析不出题 / 没配 AI Key / AI 整理失败时才一起露出来。
  ⚠️ 按钮与说明**同进同出** —— 只露按钮不说发给谁,等于把活儿甩给用户猜;
  ⚠️ 露出来之前先别让文案去指它(否则文案让用户"点复制提示词",屏幕上根本没那个按钮)。
  救援区的 `.home-fold` / `.rescue-*` 样式与 `.is-primary` / `.auto-ready` 主次机制**一并删除**(含早先遗留的
  `.rescue-box` / `.rescue-title` 死样式);那句随 Key 状态换的说明文字改挂在提示区(`updateAiSettingsBadge`)。


- **v48(2026-09-14)** 首页两处控件归位(👤 定):**「清空」→ 输入框右上角**(重新设计形状大小),
  **「?」→ 「导入题库」标题右边**。随之三处结构性调整都记在 §3.11:
  ① 输入框改成"**带角落动作的盒子**"(`.paste-box` 承担边框与聚焦环,textarea 变无边框内芯,
  角标独占一行**不浮在文字上** —— 浮着会被长行钻到下面);
  ② 首页卡头从 `space-between` 改 `flex-start`(否则标题与「?」被顶到两端,而它俩必须挨着),
  用了一年的 `.card-head-actions` 包裹随之撤掉;
  ③ 「?」面板改贴触发器**左**边(`left: 0`):触发器到了左半侧,再按右边对齐会向右溢出。
  「清空」现在是**三级动作**(hover 才露红的小药丸),下排只剩主键 + 「选择文件」。


- **v47(2026-09-14)** 「⚙ AI 设置 / 已连接 ✓」从**首页导入卡头**挪到**标题栏正中**(👤 定)。
  它是"整个 App 的 AI 配置状态",不属于导入卡片那一个模块。落地要点三条,都写进了 §3.3:
  ① 桌面档 header 改回**三栏 grid(`1fr auto 1fr`)**,只有正中的孩子才真的落在正中
  (flex + space-between 只均分**间隙**,实测徽章被推到 2/3 处:1280px 下偏 195px);
  ② 手机档仍退回 flex(nav 是底部固定条,grid 会给它保列 ⇒ 排版错乱);
  ③ 实测过两种挤法:`nav` 会被压(按钮文字溢出到徽章底下,769~860px 重叠 35px)、
  品牌小字会折行(标题栏 48→54px)—— 取舍是**导航不缩、让品牌吸收缺口**。见 §3.3 / §3.11。


- **v46(2026-09-14)** §3.14 增「参考实现」小节:👤 提的 PyMuPDF(**AGPL,代码不可抄**)/ docling(MIT 但 Python+模型)/
  Stirling-PDF(Java 服务端)三者都**无法引代码**,只能借做法 —— 已落地两条:PyMuPDF 式的**按位置排序**、
  docling 式的**重复文本清理**(`stripRepeatedOverlay`:同字同位 ≥3 行 → 候选;候选按行拼成的句子多行重复 → 整句丢,
  **要么整句清要么不动**)。三个项目与许可一并记在文档里。


- **v45(2026-09-14)** 新增闸门判据 `overlay`(👤 的第二份真文件《【四海】数量关系题本》压出来的):
  这份 PDF 把水印文字**织进每一行**(字距拉到 10 个字),和正文画在同一条基线上 —— 几何上分不开,
  抽出来正文与水印交错。★ 判据用**几何**而非文字内容(试过"重复长句"判据:目录点线会误伤正常卷子,已否):
  同一行里相邻片段空隙 >3 字的比例 >20% ⇒ 叠加层。实测:防复制题本 54.6% vs 正常卷子 0.9%。
  另:内容流里 `q/Q/cm` 必须按栈跟踪(该文件每字一层 `q…cm…BT/ET…Q`,且页首两次翻转)。见 §3.14。


- **v44(2026-09-14)** 新增 §3.14 PDF 导入(0.15.0,👤 定题):零依赖自研解析器(扫对象不看 xref、
  `/Length` 先信再验证、ObjStm、ToUnicode CMap 中文映射)+ **质量闸门**(扫描件/乱码/几乎没文字 → 不硬导入)
  + 按原因分的**四条提示**(加密件不给 AI 按钮)。抽出的文字进同一个输入框,不新增第二条导入路径。


- **v43(2026-09-13)** 救援区定名 **「AI 整理成标准格式」**,并改回"**两条路都显示 + 按情况分主次**"
  (👤:两条都留着,但要一眼看出该走哪条)。主次判据 = `aiConfigReady`(配没配 Key):
  没配 Key → 「复制提示词给聊天 AI」在主位(浅底 + 整行 48px 按钮 + 一句步骤),「一键整理」退居次席
  (白底 + 40px 按钮 + 一句"先在右上角 ⚙ AI 设置 里配 Key");配了 Key → 两者对调。
  ⚠️ **不许再藏掉任何一条**(藏了就永远发现不了另一条路),主次只靠 `order` / 底色 / 按钮尺寸;
  ⚠️ 按钮颜色都还是 `.secondary`(首页只许一个主色实心键)。实测:两条都显示后整块 365px = 43% 屏
  (仍低于历史最差的 57%),文案 146 字。见 §3.11。


- **v42(2026-09-13)** 提示词改口径(👤 定的写法):输入框提示词用 **`方式一 / 方式二`** 两条并列 ——
  `方式一：把题目文字粘贴到这里` / `方式二：点「选择文件」，目前支持 Word / PDF / txt`。
  实测手机 3 行 / 框能显示 5 行(完整显示),桌面 2 行。见 §3.11 的导入流程表。


- **v41(2026-09-13)** 修 bug(👤:"「📋 复制提示词和题目」点击后只复制了提示词,题目没有"):
  监听器写成了 `addEventListener('click', copyOfficialPrompt)` —— 浏览器**把 MouseEvent 当第一个实参**
  传进去,而那个参数是 `forcePromptOnly` 开关,事件对象是真值 → 每次都走"只要提示词"分支,静默降级。
  修法两道:① 监听器包一层箭头函数;② 开关参数改成**严格等于 true** 判断(别再相信真值)。
  顺手把 main.js 里 31 个裸挂监听器逐个核对了一遍:只有这一个的首参是开关,其余要么无参,
  要么本来就是事件(`handleFileSelect` / `editorHistClick` / `handlePasteEvent`)。见 §3.11。


- **v40(2026-09-13)** 文案去括号(👤:"去掉括号里的废话,比如（材料里没答案的题留空）（只改格式，不改内容）
  （网页/微信/word直接复制）"):首页主卡片与救援区**不再用括号塞补充说明** —— 要说的信息直说,
  说不进一句话的就别写;数据/计数用 ` · ` 分隔(`🤖 AI 整理中 2/5 块`),不塞括号。
  改后:提示词 4 行→**3 行**、救援区 267px→**246px(29% 屏)**。顺带修掉两处过时文案
  ("改用左边 A 路线" —— 现在没有 A/B 两栏了;失败分支同样要切回手动路)。
  ⚠️ 例外:格式示例 `<pre>` 里的括号是**语法本身**(`……（B）`、`（多选连写：答案：ABC）`),必须保留。
  守卫:`tests/dom-structure.test.js`(主卡片+救援区文案逐个扫 `（…）`)。见 §3.11。


- **v39(2026-09-13)** 救援区去啰嗦(👤:"太啰嗦而且重点不清楚"):实测改前**状态行 90 字 + 救援区 272 字、
  占手机 57% 屏**,两栏标题各挂一句"——只改格式,不动内容",读完仍不知道点哪个。现在:
  ① 状态行只报"出了什么事 + 往哪走"(**≤32 字**,不再复述按钮名);
  ② 救援区先砍成"只铺一条路"(⚠️ **该口径已被 v43 取代**:两条路要都留下,只分主次),
  ③ AI 一键整理没结果时切回手动那一支(这条规矩 v43 仍然有效)。
  实测:失败后救援区 479px→**267px**(57%→32% 屏)。新增文案预算守卫(救援区 ≤190 字)。见 §3.11。


- **v38(2026-09-13)** 对齐首页导入流程(👤 定口径):**提示词给出两条路(粘贴 / 选文件,并写明
  Word/PDF/txt 都能选)→ 选到 doc/pdf 给专门提示 → 解析不出题目时给提示并自动展开救援区**。
  修掉两处"文案指路指到空气里":① 解析失败只说"试试上方「复制官方提示词」",而那段是默认收起的
  `<details>`、界面上也没有这个名字 → 现在**自动展开 + 滚到救援区**,并点名真按钮;
  ② .doc 提示里引用的预览页按钮旧名「AI 兜底整理」→ 改成真名「🤖 AI 格式整理(不改内容)」。
  新增守卫:失败提示里 `「」` 引用的名字必须在 index.html 里真实存在。见 §3.11。


- **v37(2026-09-13)** 修:导入输入框(`#paste-input`)上写着 `white-space: pre`,**把自动换行关掉了** ——
  长行横向溢出、右边被裁(实测框宽 340px / 一行内容 760px,每行只看得到前半截),👤 看到的
  "提示字显示不全"就是这个。改成 **`pre-wrap`**(保留粘贴的原始换行 + 超宽行自动折行)。
  见 §3.11 新增的"输入框"条目。


- **v36(2026-09-13)** 首页参考材料改挂输入框模块:`#format-help` 常驻折叠块**删除**,改成导入卡
  右上角的圆形「?」→ 点开**悬浮面板**(`details.home-help` + `[open]` 控制显隐 + `absolute` +
  z-index 60;桌面档贴触发器右对齐,手机档锚定卡头左右贴边)。同时修掉一个**类名泄漏**:`.card-head`
  两处共用,弹窗那条规则写在文件后面,把 `justify-content:center` / `padding:0 48px 0 8px`
  (那是给弹窗 ✕ 留的位置)整套漏到首页卡头 → 右上角控件永远贴不到右上角;现在弹窗限定
  `.edit-card .card-head`,首页卡头自有一条。见 §3.11。


- **v35(2026-09-13)** 题库页两处(👤 反馈"题库滑到最下面再往上滑,底部仍然有悬浮的白条"+ "把上次导入
  放到题库最末尾,不要悬浮,和题库卡并列"):① **手机档库列表解除内滚**(原来页面 + 列表两个滚动区,
  滑到底会剩一块框下空白 + 底部 tab 栏 = 那条"白条");② **「上次导入」行从列表上方挪到列表最末尾**,
  与库卡同列同层级。新增 §3.13。


- **v34(2026-09-13)** 「加到主屏」引导从**题库页底部**挪到**首页最后一块**(👤:那东西在题库页
  就是一条"悬浮的空白栏";装 App 的时机在首页)。同宽规则补 `#home-section > .install-hint`,
  守卫改为"必须在首页内、题库页内不许出现"。见 §3.10 / §3.11。


- **v33(2026-09-13)** 修:编辑器工具条吸顶时上方露出一条缝(滚动容器的内边距没被抵消)——
  内边距提成变量 + 吸顶条负 `top`/负 `margin` + 自身铺满底色。见 §3.5。


- **v32(2026-09-13)** 撤销体系定稿.(0.14.1)两层分工(撤销栈会话内 / 版本记录跨会话)、
  导入也进版本记录、槽位 3→5 与 10→20、删掉"撤销上次导入"(批次级撤销语义脆弱)、
  学习痕迹规则(题库操作不动错题/收藏,只有删库打包).新增 §3.12。


- **v31(2026-09-13)** 首页重构(👤:简洁易读、主次得当、手机优先):三层结构(主任务 / 补救路径折叠 / 参考折叠)、
  全页唯一主色实心按钮、撤销导入收成卡内一行。新增 §3.11;修掉一处"两个蓝按钮"的暗桩(`#upload-btn` 的 id 特判)。


- **v30(2026-09-12)** 新增 §3.10:PWA manifest + 图标(圆角方底 + 白色 Z、maskable 安全区、touch icon 不透明、
  `start_url/scope` 必须相对路径、`theme_color` 取页面底色);明确本批**不含** service worker。


- **v29(2026-09-12)** 修:配色卡片的脚部键(错题/收藏)展开后"白字压白底"看不见 —— 半透明白底的覆盖漏了`:not(.open)`。见 §3.4。


- **v28(2026-09-12)** 「导出题库」逐库写分节标题、**导入时自动认出多题库**(默认分开导入);回收站点外面收起。新增 §3.9。

> **只留最近几条**(每条一两行);完整过程记录(含更早版本)在 `archive/DESIGN-history.md`。
> 本文件只保留**现在仍然有效的规则**,规则写在正文各章,这里只说"这轮改了什么"。


- **v27(2026-09-12)** 列表交互定稿:**单击高亮 / 双击进编辑卡片**;去掉列表行与编辑卡片的左侧色条(编辑卡片原名 `.question-card` 撞上错题卡的红条类名,已改名 `.edit-card`)。⚠️ 单击**只换高亮不重画列表**,否则 `dblclick` 永远不触发。**设计规范自身也做了一次瘦身**:修订记录归档 + §3.5/刷题状态栏两章重写为当前口径。

- **v26(2026-09-12)** 单击高亮/双击选中/复选框圈选喂「只看勾选」;批量栏去掉「编辑」键;工具行 sticky 悬浮;筛选面板移出可横向滚动的工具行(否则被裁)。

- **v25(2026-09-12)** 控制条按钮矮一档(工具键 24/34、chip 22/30);筛选面板改悬浮;手机列表取消内滚;桌面删掉 220px 两栏残留;批量栏并入工具行;删掉"共 N 题"。

- **v24(2026-09-12)** 题目列表重构(复选框 + 徽章 + 筛选 + 批量栏),六个字段搬进编辑卡片(二级弹窗 1100)。

- **v23(2026-09-12)** 题库页头部一行三键(标题 · 导出题库 · ＋ 创建题库 · 🗑 回收站),三键同款胶囊、只有创建键实心。

- **v22(2026-09-12)** 「导出全部」→「导出题库」,搬到题库页头部(创建键左边)。

<details><summary>更早的版本(一行式索引;完整记录见 archive/DESIGN-history.md)</summary>

- v21(2026-09-12) 题库设置重新排版:按作用对象分块 + 删除题库独占危险区
- v20(2026-09-12) 删掉「当前题库:xxx」;修"改名后编辑器不刷新"
- v19(2026-09-12) 「保存」跟着选中的那一道走(行内 ✕/保存)
- v18(2026-09-12) 修:二级弹窗压在一级之上;删库后回收站当场刷新
- v17(2026-09-12) 回收站改成「我的题库」右侧的悬浮菜单
- v16(2026-09-11) 版本记录:补删除单条、修正存版时机、迁入题库设置
- v15(2026-09-11) 表单文案在输入框左边;判断题 A=正确/B=错误
- v14(2026-09-11) 编辑器再收敛:取消底栏/上一题下一题/全部折叠
- v13(2026-09-11) 编辑器两标签页 + 头部收矮 44px
- v12(2026-09-11) 题库编辑器重构(手机优先)
- v11(2026-09-11) 题库卡改成"配色底 + 框"(去掉左侧 4px 主色条)
- v10(2026-09-11) 错题卡与收藏卡合并为 .question-card
- v9(2026-09-11) 题干上方标签行统一高度(--tag-row-h)
- v8(2026-09-11) 修:手机上题干被压成窄柱(auto 边距吃空间)
- v7(2026-09-11) 修:状态栏按钮文案变宽把整条挤高
- v6(2026-09-11) 修:套题模式交卷前答题卡泄露对错
- v5(2026-09-11) 刷题状态栏定稿(收藏移到标签行)
- v4(2026-09-11) 答题卡抽屉规范
- v3(2026-09-10) 色彩令牌全量重校准
- v2(2026-09-10) 采纳 slate/emerald/rose/amber 色阶,卡片 12px + 细边框 + 微阴影
- v1(2026-09-10) 首版

</details>

